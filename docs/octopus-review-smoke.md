# Octopus review smoke test

This short-lived file exists only to verify that the self-hosted Octopus reviewer is triggered correctly on an ED-Finder pull request after the new production deployment was completed.

Once the end-to-end review path is confirmed, this file should be removed rather than merged into `main`.
