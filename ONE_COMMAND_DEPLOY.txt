# 🚀 ED:FINDER - ONE COMMAND DEPLOYMENT

## ⚡ **Fastest Way - Just Copy/Paste This**

### **Open PuTTY → Connect to your Pi → Paste this:**

```bash
cd ~ && wget https://www.genspark.ai/api/files/s/YoHgRiiX -O ed-finder-WORKING.tar.gz && tar -xzf ed-finder-WORKING.tar.gz && cd ed-finder-WORKING && ./DEPLOY.sh
```

**That's it!** Wait 2-3 minutes for it to complete.

---

## 🌐 **Then Open Your Browser**

**URL:** http://raspberrypi.local

### **MUST DO: Clear Cache**
1. Press `Ctrl+Shift+Delete`
2. Select "All time"
3. Check "Cached images and files"  
4. Click "Clear data"
5. Press `Ctrl+Shift+R`

---

## ✅ **You Should See**

- ✅ Green status light
- ✅ All tabs work
- ✅ Search works

---

## 🔧 **If Port 80 Busy**

```bash
sudo systemctl stop influxd && cd ~/ed-finder-WORKING && docker compose restart web
```

---

## 📦 **Package Details**

- **Download:** https://www.genspark.ai/api/files/s/YoHgRiiX
- **Version:** v3.18 (complete working frontend)
- **Size:** 234 KB

---

## 💡 **Why This Works**

The `-O ed-finder-WORKING.tar.gz` flag tells wget to save the downloaded file with the **exact name** that `DEPLOY.sh` expects, so there's no filename mismatch.

---

**That's all you need!** 🎉
