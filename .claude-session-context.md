# Real-Star LOD Streaming Debug Session (2026-08-15)

## Problem
Phase 2 of real-star LOD streaming was deployed. Visually: **no real stars appear when zoomed in**, and **heatmap looks degraded when zoomed** (stretching/pixelation).

## Architecture
- Hook `useViewportSystems` (viewportSystems.ts) — detects zoom, fetches viewport bbox, manages debounce/settling
- Component `RealStarLayer` (map-foundation/RealStarLayer.tsx) — renders the fetched systems
- Parent `ProductionMapTab` (map-foundation/ProductionMapTab.tsx, line 265) — calls hook, passes data via `productionOverlays.realStars` (line 646) to `R3FMapFoundation`
- Renderer `SceneContents` (map-foundation/SceneContents.tsx, line 397) — renders `RealStarLayer` IF `realStars` exists and has length

## Wiring is Correct
- Line 265: `const viewportSystems = useViewportSystems({ camera: scene.camera, viewport });`
- Line 646: `realStars: viewportSystems.systems` passed to `R3FMapFoundation`
- SceneContents line 397-406: conditional render `{realStars && realStars.length > 0 && <RealStarLayer ... />}`

## Current Investigation

**Suspect: Hook not toggling `detailEnabled` on zoom**
- RAF polling with `[enabled]` dependency may not be capturing prop updates correctly
- Or zoom thresholds aren't being hit (REAL_STAR_ENTER_MAX_LY = 12,500)

**To verify:**
1. Open browser DevTools → Console, zoom in on the map
2. Look for `[viewport-systems] camera key: ...` messages (line 172 in hook)
3. Check Network tab for GET `/api/map/systems` requests
4. Check if `detailEnabled` ever becomes true

## Files in Play
- `frontend/src/features/map/viewportSystems.ts` — hook (currently has RAF monitoring issue being fixed)
- `frontend/src/features/map-foundation/RealStarLayer.tsx` — component
- `frontend/src/features/map-foundation/SceneContents.tsx` — parent (also does fade opacity logic)
- `frontend/src/features/map-foundation/ProductionMapTab.tsx` — wiring point
