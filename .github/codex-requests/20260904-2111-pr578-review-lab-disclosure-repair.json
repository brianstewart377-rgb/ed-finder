{
  "task": "Repair the exact remaining Review Lab failure on existing PR #578 branch migration/svelte-cypress-hard-cut. Start by reading CLAUDE.md, frontend/cypress/e2e/review-environment.cy.js, frontend/src/features/colony-planner/WarehouseEvidenceCard.tsx and tests/test_local_review_test_environment.py. The latest full Review Lab report on branch head cfa9f9eebdf6bd7d78c6563fcb03e51bbeef02fa passed static checks, API contracts, keyboard accessibility, console policy and teardown, but the browser journey stopped in Gamma before Delta. Its fatal error was: Timed out after 8000ms finding [data-testid=\"warehouse-evidence-technical-details\"] at review-environment.cy.js around line 168. Because Gamma aborted, Delta never exercised the already-added ordered warehouse-evidence 503 -> provenance 200 fallback, producing the misleading top-level DELTA_FALLBACK_NOT_TRIGGERED result. Implement a narrow, fail-closed fix: root technical-details discovery inside the current planner-warehouse-evidence surface; inspect the real rendered WarehouseEvidenceCard markup and support only legitimate native disclosure variants actually present; re-query after React rerenders rather than holding stale elements; activate through native keyboard/click semantics as appropriate; positively prove the intended disclosure is open/expanded before asserting its contents; and make absence or ambiguity fail with a useful assertion. Preserve the existing API intercepts and ordering proof, all five viewport profiles, all four evidence postures, keyboard/Escape/overflow checks, browser-summary.json schema, report-only/canonical authority language, and every unrelated product behaviour. Add or strengthen a focused regression contract in tests/test_local_review_test_environment.py so the old brittle global selector cannot return unnoticed. Run the focused Python contract tests and the smallest relevant frontend/Cypress checks; run the full Review Lab verification if practical. Do not change application product markup merely to satisfy the harness unless the markup itself is demonstrably defective. Do not deploy, access production, or use production credentials. Keep the diff tightly scoped and leave the branch ready for fresh PR checks.",
  "mode": "implement",
  "target_branch": "migration/svelte-cypress-hard-cut"
}
