{"mode":"implement","target_branch":"release/v3-application-foundation","task":"Implement the immutable V3 application release/deployment foundation on existing branch release/v3-application-foundation, created from current main 7102ec1cf706020ea8268edd595df6d5e52c0006. Read CLAUDE.md, README.md, docs/development/v3-application-stack-decision.md, docs/operations/infrastructure-status.md, docs/operations/operator-command-contexts.md, scripts/operator/README.md, the current chatgpt-ed-new-ops workflow and action scripts, apps/api Dockerfile/config/health/auth/OpenAPI routes, apps/web build/static fallback configuration, nginx boundaries, schema migration ledger, CI/action pinning conventions, and tests. Treat these production facts as authoritative: host ed-finder-prod / nb79a3d.mevnode.com; PostgreSQL 18 and support containers healthy; origin 127.0.0.1:58080 health 200/database connected from stale edfinder-v3-api:phase4c-r5 with build_sha unknown; origin lacks /openapi.json and current /api/auth/*; no frontend index; public health 503; public anonymous session responds; app containers edfinder-v3-api, edfinder-v3-proxy and edfinder-v3-public-auth-edge; host checkout is historical and must not be a release artifact. A safe runtime-contract export correctly failed closed because old docker-compose.yml contains credentialed URIs and sensitive environment assignments. Do not weaken that scanner, export old Compose, print secrets, or copy sensitive config off-host. Build a production-grade release foundation that deploys the final Svelte head without builds or git pull on production. Use an immutable API image and immutable nginx web/proxy image serving apps/web adapter-static output while leaving public-auth-edge, PostgreSQL 18 and support/data containers untouched. Add a reproducible multi-stage apps/web production Dockerfile using Node 24, pnpm 11 and frozen locks, plus hardened nginx with exact routing: /api/*, exact /openapi.json and numeric /s/{id64} to FastAPI; all other application/static routes stay with Svelte and use the deliberate SPA fallback. Preserve GET/HEAD, asset caching, query strings, malformed/encoded-path fail-closed behaviour, security headers and no backend catch-all. Harden the API image for immutable provenance: exact 40-hex BUILD_SHA, host-neutral APP_VERSION, /api/health reports both and production identity cannot be unknown; do not bundle frontend into API. Add testable mounted-secret/*_FILE handling for database URL, Redis/Valkey URL, admin token, Frontier client secret and telemetry DSN while preserving nonproduction env compatibility; no secret may be a frontend build input. Add a clearly named dedicated V3 production Compose/runtime authority, never root docker-compose.yml. It must use immutable image identity from a release manifest, contain no inline secrets, reference rather than recreate external PostgreSQL/support services, and recreate only selected V3 app services. Implement fail-closed host topology discovery using docker inspect that records only non-secret network/port/mount/container metadata and never environment values; require exact host/FQDN and healthy prerequisites. Define a versioned release-manifest generator/validator recording Git SHA, API/web image IDs or digests, Dockerfile/lock/config hashes, SQL migration-set identity, current/required schema compatibility evidence, route-contract version, secret file names/targets without values, run/timestamp, prior release and rollback eligibility. Never apply migrations automatically; rollback is false unless current-schema compatibility is positively proved. Extend the governed ed-new operator plane with a narrowly allowlisted v3-app-deploy and useful explicit status/rollback operations. Validate request JSON strictly, including exact release_sha. Checkout trusted main, require the requested SHA to equal current accepted main or a documented safer protected-head rule, build/test images on the GitHub runner from frozen inputs, create/validate manifest, transfer OCI archives and a sanitized runtime bundle over pinned SSH. Production may only docker load/pull immutable images, stage an exact release directory, preserve host-only secrets and run the reviewed Compose authority; no source update, dependency install, build, registry-token leak, DB write, migration, V2 restore or unrelated restart. Provide idempotent two-phase host deploy: preflight hashes/images/host/Docker/Compose/topology/secret permissions/read-only live schema identity/current release/rollback compatibility; stage without disruption; switch only API and web/proxy while retaining public edge and data/support; then verify containers, exact SHA, origin/public health, OpenAPI, anonymous session, all auth routes, root shell, representative deep routes, assets, API boundary and numeric share route. Commit current-release pointer only after all checks. Automatically restore only an eligible schema-compatible prior app release; otherwise fail closed with a sanitized receipt. If needed, provide a separate deliberate on-host secret bootstrap that reads only an explicit allowlist from the current API container and writes root-owned 0600 files without printing/uploading/overwriting values; never claim Frontier configured when absent. Emit sanitized build/deploy/rollback receipts and short-retention artifacts. Add regression tests for request rejection, secret absence, no host build/git pull, service allowlist, host guard, digest/SHA/schema validation, atomic release pointer, rollback fail-closed, nginx ownership, Docker/Compose syntax, image provenance, *_FILE resolution and health identity. Run affected Python/V3/decommission/operator tests, apps/web check/lint/format/test/build, container builds, nginx -t, compose config with dummy secret paths, secret scans and git diff --check. Do not deploy or access production, mutate DB/OAuth/Stage27, merge branches, or weaken recovery scanning. Leave a focused PR-ready branch and document the exact one-time operator sequence after the complete Svelte head is merged."}