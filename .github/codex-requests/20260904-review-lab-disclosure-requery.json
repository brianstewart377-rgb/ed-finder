{
  "task": "On the exact latest tip of migration/svelte-cypress-hard-cut, make one narrowly scoped Review Lab harness repair. The latest exact-head browser artifact shows frontend/cypress/e2e/review-environment.cy.js failing in technical(): it captures the current [data-testid=warehouse-evidence-technical-details] element, clicks its summary, React re-renders, then asserts against the detached old details node. Rewrite technical() so it re-queries the details element after any click before asserting open. Preserve the current status/posture assertions and the fallback disclosure-toggle branch. Do not change application behaviour, evidence wording, Python evaluator schema, keyboard-default-action code, dependencies, workflows, backend, Svelte code, or any production surface. Add or update a focused non-networked Python repository contract proving technical() does not reuse a pre-click details subject for the post-click assertion. Run the focused contract test, git diff --check, and any inexpensive relevant static check. Push the resulting commit directly to migration/svelte-cypress-hard-cut with exact-head lease. No production, database, deployment, recovery, secret, or live-data action.",
  "mode": "implement",
  "target_branch": "migration/svelte-cypress-hard-cut"
}
