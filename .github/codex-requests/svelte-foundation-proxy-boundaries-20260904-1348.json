{
  "task": "Resolve the final known Codex P2 on PR #576. Target branch frontend/svelte-foundation at exact current head 506308f89f5a38e92a07a8e9bbf06e264575f36e. Scope strictly to boundary-correct Vite dev/preview proxy matching plus focused regression coverage. Current apps/web/vite.config.ts uses '/api', '^/openapi\\.json$', '^/s/\\d+$'. Fix it so backend-owned same-origin routes are matched correctly when Vite tests the full request URL: /api and /api/... (including query strings) must proxy, but lookalikes such as /apiary must not; exact /openapi.json with or without a query string must proxy; numeric /s/{id64} with or without a query string must proxy, while nonnumeric or extra-path lookalikes must not. Prefer explicit boundary-aware regex rules rather than broad startsWith keys. Add/adjust focused tests (unit/config contract and/or Cypress) that prove at least /apiary is not claimed by the backend proxy and that /openapi.json?format=json plus /s/0?utm=x still reach FastAPI. Preserve all current Svelte foundation behavior and the release-age policy. Run clean validation: Node 24/pnpm 11.25.0 frozen install, pnpm check, pnpm lint, pnpm format:check, pnpm test, pnpm build, and the Svelte Cypress foundation smoke against the disposable FastAPI service if the existing workflow harness makes that practical; at minimum run the relevant repository contract tests with python -m pytest tests/test_svelte_web_foundation_contract.py -q. Do not change dependencies, lockfile, OpenAPI generation architecture, backend runtime, deployment, React reference behavior, or Babylon/Stage 27 authorization. Commit and push only the proxy-boundary fix and focused tests to frontend/svelte-foundation using the worker's trusted exact-head mechanism.",
  "mode": "implement",
  "target_branch": "frontend/svelte-foundation"
}
