{
  "mode": "implement",
  "task": "Implement a corrected read-only Octopus edge-status operation on current main, incorporating the verified findings from PR #547 rather than merging its flawed version. Read CLAUDE.md and the existing ed-new ops patterns first. The target is MevSpace hostname ed-finder-prod / FQDN nb79a3d.mevnode.com, Octopus v1.0.122. Add/retain a narrowly scoped `octopus-edge-status` operation in `.github/workflows/chatgpt-ed-new-ops.yml`, `scripts/operator/actions/dispatch.sh`, a dedicated `scripts/operator/actions/octopus-edge-status.sh`, operator README, and focused tests. Requirements: strictly read-only, fail-closed, no DB access, no .env/secret contents, no private-key reads, no service restarts/writes. Report listeners 80/443/43300, bounded relevant containers, Octopus internal health/version, relevant proxy routing, DNS, HTTP/HTTPS, and served certificate names. IMPORTANT verified review findings to fix: (1) production nginx may route Octopus through a variable such as `set $octopus_upstream http://octopus-web:3000;` then `proxy_pass $octopus_upstream`; recognize this Docker-network route correctly and do NOT require a literal 43300 route. Safely capture only relevant `set`/`server_name`/`proxy_pass` directives and correlate variable definition/use. (2) Keep the certificate actually served on 443 separate from certificates merely installed under `/etc/letsencrypt/live`; `certificate_present` / served-certificate validity must be based on the certificate served for SNI `octopus.ed-finder.app`, while installed certificate availability may be a separate informational field. Do not use installed certs to make a served-cert check pass. (3) preserve a bounded `/api/version` response and validate it actually identifies expected release `1.0.122`; endpoint HTTP success alone is insufficient. (4) nginx/proxy inspection errors must fail closed or be explicitly surfaced as inspection failure; do not suppress `nginx -T`/docker exec failure and then report `route_present:false` as if inspection succeeded. Keep `curl -k` only if separately validating served certificate metadata; receipt should distinguish HTTPS response from certificate validity. Add tests that specifically cover these four findings plus read-only/secret-safe allowlisting. Do not access production or deploy; only implement/test repository changes for later merge and execution through ChatGPT ed-new Ops."
}