{
  "task": "Implement WP5 from the accepted planner/simulation migration dossier on the exact latest tip of migration/svelte-simulation-preview, created from the green migration/svelte-cypress-hard-cut head after WP4. Read CLAUDE.md, docs/ROADMAP.md, docs/development/v3-application-stack-decision.md, issue #580, PR #578, current Svelte platform/domain/persistence/routing/evidence code, all mounted React SimulationPreview/optimiser/planner integration code and tests, relevant FastAPI simulation endpoints/models, and Review Lab acceptance behaviour before editing. Port the accepted Simulation Preview capability to Svelte as an explicit, testable state machine rather than translating React effects. Define typed states/events/guards/commands for at least: no plan/input; invalid input; ready/current input; simulation requested; running; current success; stale success after any simulation-relevant input change; error/retry; cancelled/superseded request; optimiser requested/running; candidate results; candidate selection; atomic candidate application; candidate application rejected because the base fingerprint changed; and recovery after navigation/project reload. Use the pure WP1 simulation fingerprints, validation and immutable snapshot helpers as authority. Assign monotonically increasing request generations or equivalent abort/version guards so late simulation or optimiser responses can never overwrite newer state, a different system/project or changed placements. Keep server response/result data derived and non-canonical: simulation results and optimiser candidates must not mutate/save canonical plans until an explicit user Apply command succeeds against the exact base fingerprint/version. Applying a candidate must be atomic, idempotent against duplicate activation, preserve stable placement identifiers/resequencing rules, invalidate the old simulation result exactly once and expose an undo/revert boundary if accepted React behaviour provides one. Evidence/provenance remains passive and must not trigger simulation/optimiser or alter inputs. Build a Svelte-owned service/controller using plain command functions and focused stores/context; use `$derived` only for genuine derivations such as current/stale/readiness and avoid broad `$effect` orchestration. Use the lossless API facade, same-origin credentials, structured ApiError, canonical decimal-string id64, bounded retries and system/project/fingerprint-scoped TanStack query keys where appropriate. Implement the mounted Simulation Preview UI and integrate it into the canonical planner preview route/shell only to the extent needed for an independently usable vertical slice; do not build the full Colony Planner workspace in this packet. Preserve accepted controls, summaries, validation/warnings, loading/error/empty states, candidate comparison, keyboard operation, focus/announcements and constrained viewport behaviour. Add exhaustive reducer/state-machine unit tests for every transition and forbidden transition; out-of-order/deferred-promise tests; duplicate-apply and fingerprint-mismatch tests; parity fixtures against React simulation/optimiser tests; component tests; and Cypress journeys for run, stale-after-edit, retry, superseded response, optimiser, apply-once, keyboard-only and reload/navigation recovery. Add repository contracts proving no direct effect-driven API loop, no unsafe id64 numeric coercion, no evidence-triggered simulation and no result/candidate direct write to canonical persistence. Extend Review Lab only with fields/assertions genuinely required by the existing schema/accepted preview journey; do not weaken or fabricate evidence. Do not change game mechanics, backend/database schema, invent API fields, port the full planner, port My Work/Compare/journal/EDDN/Admin/Operator, delete React, restore Playwright, modify release/production authority, deploy, access secrets/live data or create a root JS monorepo. Run apps/web install/check/lint/format/test/build, focused Simulation Preview Cypress journeys, full Review Lab collector/evaluator, relevant React parity tests, backend simulation API tests and Python repository contracts. Push the complete bounded result directly to migration/svelte-simulation-preview through the exact-head lease. End with the state/event table, API endpoints/query keys, stale/supersession/apply guarantees, parity tests mapped, checks/results and deliberately deferred planner integration risks.",
  "mode": "implement",
  "target_branch": "migration/svelte-simulation-preview"
}
