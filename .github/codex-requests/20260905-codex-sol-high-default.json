{"mode":"implement","task":"From the exact current origin/main, make a governance-only Codex worker change: keep the required model exactly gpt-5.6-sol, but change the normal reasoning effort from max to high for BOTH investigation and implementation runs. Update .github/workflows/codex-laptop.yml so its constants, CODEX_ATTESTATION output, and both codex exec --config arguments all agree on high. Update tests/test_codex_model_governance.py and the authoritative current control-plane documentation so they assert and describe high, not max, as the normal Contabo default. Preserve --ignore-user-config, --strict-config, fail-closed model selection, no fallback, the three-run self-hosted pool, immutable request routing, exact-head leases, and every security boundary. Do not add an untrusted per-request model or effort override. State that max is exceptional and requires a separate owner-approved governance change, rather than being silently selectable by task text. Change no application, frontend, backend, database, deployment, or production behaviour. Run the focused Codex governance/async-bridge tests, YAML parse, repository state resolver, ruff for touched Python, and git diff --check. End with exact files changed and results; the wrapper will commit and push the isolated branch."}