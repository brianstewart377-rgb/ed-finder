{
  "task": "Remediate PR #576 on target branch frontend/svelte-foundation at exact current head a827575247792d31fe56b5df249eccb2fdc42846. Keep scope strictly to the two diagnosed CI failures. First, in apps/web run the locked Prettier toolchain so pnpm format:check passes; CI identified these unformatted files: cypress/e2e/foundation.cy.ts, openapi/bootstrap.openapi.json, src/app.css, src/lib/components/BootstrapStatus.svelte, src/lib/components/TestShell.svelte, src/routes/[journey]/+page.svelte, src/routes/+layout.svelte, and src/routes/+page.svelte. Do not hand-change behaviour while formatting. Second, restore README.md authority wording required by tests/test_v3_application_stack_decision.py: the normalized README must contain the exact phrase 'locked target for new V3 application implementation is Svelte 5/SvelteKit 2/TypeScript 6'. Preserve the new apps/web/Svelte foundation documentation and do not weaken or change the existing stack-decision test expectation. Run focused validation: from apps/web pnpm generate:api then git diff --exit-code -- src/lib/api/generated, pnpm check, pnpm lint, pnpm format:check, pnpm test, pnpm build; and from repo root run python -m pytest tests/test_v3_application_stack_decision.py tests/test_svelte_web_foundation_contract.py -q. Do not change dependencies, lockfile versions, backend runtime, deployment, React reference behaviour, or Babylon/Stage 27 authorization. Commit and push only the bounded remediation to frontend/svelte-foundation using the worker's trusted exact-head mechanism.",
  "mode": "implement",
  "target_branch": "frontend/svelte-foundation"
}
