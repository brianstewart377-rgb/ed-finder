{"mode":"implement","target_branch":"migration/svelte-cypress-hard-cut","task":"Repair the exact current Review Lab failure on draft PR #578 without weakening the Cypress-only hard-cut. Work on existing branch migration/svelte-cypress-hard-cut at expected head 2afae6477db57a22b9100ff893acaac39d9537b8; do not create a PR. Evidence from run 33888348941 / screenshot shows the Start a plan button is focused, `cy.press(Cypress.Keyboard.Keys.ENTER)` runs, but the button is not activated and `[data-testid=\"plan-start-panel\"]` never appears. Replace this failing global key command with a subject-bound Cypress keyboard activation that genuinely exercises Enter on the focused button (for example `cy.focused().type('{enter}')` after asserting the exact visible `open-plan-start` button has focus), then assert `plan-start-panel` is visible before setting `alphaKeyboardOpenPlannerWorks`. Keep an explicit contract that the keyboard branch contains no mouse `.click()` fallback. Update tests/test_local_review_test_environment.py accordingly so it protects the working keyboard path rather than the ineffective `cy.press` spelling. Also remove the deprecated `allowCypressEnv` Cypress config option while retaining the trusted Node-task handshake and add/update a contract that browser specs do not read Review Lab authority from `Cypress.env()`. Bring `.github/workflows/cypress-parity.yml` and tests/test_ci_dependency_contract.py into alignment with main commit de0899761e3968bb0ae080448d77091cf8d3598d: immutable actions/cache v6.1.0 SHA 55cc8345863c7cc4c66a329aec7e433d2d1c52a9, no old Node20 cache SHA, and no ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION. Run focused Review Lab Python tests, frontend lint/type/unit checks, Cypress Review Lab spec against the disposable stack or `scripts/dev/review_environment.py verify --mode full --scenario all --confirm-local-review-environment` where feasible, and git diff --check. Do not alter the Python-owned browser-summary schema/evaluators, do not drop Delta fallback, viewport, keyboard, overflow, API, console, accessibility, Firefox, axe or visual assertions, and do not deploy/access production or secrets. Commit and push the coherent fix to the existing branch."}