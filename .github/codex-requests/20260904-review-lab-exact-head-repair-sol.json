{
  "task": "On the exact latest tip of migration/svelte-cypress-hard-cut, repair the remaining Review Lab failure without broadening scope. First fetch and inspect the latest exact-head Review Lab job logs, browser-summary.json, screenshots/video, current frontend/cypress/e2e/review-environment.cy.js, tests/test_local_review_test_environment.py, and the relevant evidence components. The current head already waits for warehouse evidence and ordered Delta provenance fallback; do not remove that protection. The observed remaining class of failure is disclosure lookup/activation in technical(): it may retain a pre-click React subject or match a transient/wrong evidence surface. Root the entire technical disclosure operation inside the visible [data-testid=planner-warehouse-evidence] card, support only the legitimate disclosure variants used by the application, re-query after any click/rerender before asserting open, and positively verify the intended card's disclosure is open and contains the expected technical content. Preserve the five viewport profiles, Alpha/Beta/Gamma/Delta evidence postures, native keyboard-only planner entry, API correlation, console/page-error capture, accessibility, overflow, screenshots, Python-owned browser-summary schema/evaluator, safe teardown, and leak assertions. Do not use .click() as a keyboard fallback, skip/soften/fabricate an assertion, restore Playwright, alter application behaviour, change evidence wording, touch Svelte/backend/workflows/dependencies, or access production/secrets/live data. Add or update a focused non-networked Python repository contract that proves technical() scopes to planner-warehouse-evidence, does not reuse a pre-click details subject, and re-queries before the post-click open assertion. Run the focused Python contract, git diff --check, and the disposable Review Lab collector/evaluator if locally available. Push the resulting bounded commit directly to migration/svelte-cypress-hard-cut using the wrapper's exact-head lease. End with root cause, files changed, checks and evidence, and remaining risk.",
  "mode": "implement",
  "target_branch": "migration/svelte-cypress-hard-cut"
}
