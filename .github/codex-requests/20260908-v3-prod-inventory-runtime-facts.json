{"mode":"implement","target_branch":"codex/run-34202782475-1","task":"Update PR #628 branch only. Do not touch production and do not authorize promotion. Fix the read-only inventory so the committed blocker `production_promotion_cpython314_runtime_unproved` can actually be resolved from the sanitized inventory receipt instead of requiring an ad-hoc host check. In `scripts/operator/v3_production_inventory.py`, report bounded secret-safe host runtime facts: whether `python3.14` exists, whether it is CPython exactly 3.14, its bounded version string, and the existing default `python3` runtime/version used for inventory. Do not install anything. Also report the read-only Docker context fact needed by `production_local_docker_context_authority_missing`: inspect context `default` and expose only context name plus Docker endpoint/host, with no credentials/config content. Where straightforward, derive bounded loopback origin ownership for ports 58080/58081 from the already-collected Docker port data so the edge/topology review has explicit evidence. Keep target-authority.json status `stopped` and do not fill any blocker automatically. Update focused tests and runbook wording so they require these inventory facts. Preserve the no-secret/no-container-env/no-write boundary. Run the focused production deployment test and relevant lint/syntax validation. Commit and push only through the governed Codex lane; do not merge or execute production operations."}