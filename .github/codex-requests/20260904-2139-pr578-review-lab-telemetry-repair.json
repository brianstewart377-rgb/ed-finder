{
  "task": "Repair the next exact-head Review Lab failure on existing PR #578 branch migration/svelte-cypress-hard-cut, now at 3d4a585f756f2e686250f4e60f9a69ba93ea76ae. Read CLAUDE.md, frontend/cypress/e2e/review-environment.cy.js, frontend/src/features/colony-planner/WholeSystemColonyPlanner.tsx, the retained Playwright review collector as behavioural evidence, scripts/dev/review_lab browser evaluators, and tests/test_local_review_test_environment.py. The preceding commit successfully fixed the evidence disclosure problem: on Review Lab run 33921467545 all four desktop scenarios alpha/beta/gamma/delta are recorded passed, Delta recorded the expected warehouse evidence 503 followed by provenance 200, API/console/accessibility/static/teardown phases passed, and no unexpected errors appeared. The remaining fatal error occurs only after those scenarios: `Timed out retrying after 8000ms: Expected to find element: [data-testid=\"planner-telemetry-dock-toggle\"]:visible, but never found it` in telemetry() around line 300. The failure screenshot shows the Delta planner still mounted at 1440x900 with the telemetry rail content present in the right column, suggesting the collector is using a brittle visibility/current-instance/scroll-state assumption rather than a missing product route. Implement a narrow fail-closed collector repair. Scope the lookup to exactly one current visible whole-system-colony-planner and exactly one planner-telemetry-region; prove the region and native button semantics/linkage; reset or expose the relevant sticky/overflow scroll state deterministically if required; scroll the actual control into view; avoid the global `:visible` selector filter; re-query the control after every React state transition rather than retaining a possibly stale subject; focus it, activate through native keyboard Enter semantics, and positively prove aria-expanded changes false->true->false plus controlled panel state/linkage. Do not pass by invoking React handlers or mutating aria attributes directly. Preserve the entire 5x4 matrix, evidence disclosure fix, ordered Delta fallback proof, overflow and Escape checks, and browser-summary.json schema. Also improve fail-closed failure attribution so the currently executing viewport profile is written to profileResults as failed before the summary is emitted; do not turn a failed profile green or continue in a way that masks the first error. Add focused Python contract tests that reject the old global `[data-testid=...]:visible` lookup, stale chained subject reuse, missing region/control uniqueness/linkage assertions, and failure summaries with an empty profileResults map. Run the focused Python tests and full Review Lab verification if practical. Do not change product markup/CSS unless first-hand runtime evidence proves an actual accessibility defect; do not alter unrelated application behaviour, deploy, access production, database, secrets, OAuth or Stage 27. Keep the diff limited to the Cypress collector and its contract tests, update the existing branch through the governed worker, and leave it ready for fresh exact-head checks.",
  "mode": "implement",
  "target_branch": "migration/svelte-cypress-hard-cut"
}
