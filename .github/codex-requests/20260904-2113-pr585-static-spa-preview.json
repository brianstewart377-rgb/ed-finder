{
  "task": "Implement the outstanding static Svelte cold-route blocker on existing PR #585 branch migration/svelte-platform-contracts. Work from the exact current remote branch head; a prior hosted Codex comment described a local commit fb22cf7c... but that commit does not exist in the repository and must not be assumed or referenced as implemented. Read CLAUDE.md, PR #585 context, apps/web/svelte.config.js, apps/web/src/routes/+layout.ts, apps/web/package.json, apps/web/vite.config.ts, the Svelte Cypress configuration/specs, and every workflow or script invoking pnpm preview. The observed defect is a production-representative built-preview HTTP 404 for a cold request to /system/18446744073709551615 despite apps/web/build containing 200.html. Add a small dependency-free Node 24 static preview/server contract under apps/web/scripts, preserving or atomically updating the existing pnpm preview --host/--port/--strictPort invocation. It must serve existing static files with correct content types; map frontend-owned unknown/deep routes to 200.html with HTTP 200; implement GET and HEAD correctly; reject malformed percent encoding, null bytes, backslashes and traversal safely; keep query strings out of filesystem resolution; proxy only backend-owned /api and /api/*, exact /openapi.json with optional query, and numeric /s/{id64} with optional query to an explicitly supplied disposable API target; keep near-prefixes such as /apiary, /openapi.jsonx, /openapi.json/extra, /s/not-a-number and /s/1/extra frontend-owned; stream request/response bodies and safe headers without credentials or a production host; fail closed for backend-owned requests when no explicit API target exists; and exit cleanly on termination. Do not restore SSR, enumerate arbitrary id64 paths, or make Cypress accept 404. Add focused unit/contract coverage for route classification and safe path resolution. Extend the built-preview Cypress suite to prove cold direct navigation and refresh for /system/18446744073709551615 and a representative nested /colony-planner route, /apiary remaining frontend-owned, exact /openapi.json?format=json and numeric /s/0?utm=x reaching FastAPI, and malformed/traversal-like paths failing safely without exposing files. Keep README/proxy ownership rules consistent. Do not suppress Cypress allowCypressEnv or action-runtime warnings; fix bounded unsupported Cypress.env browser usage where practical or record a concrete remaining warning. Run pnpm check, lint, format check, unit tests, build, and the Svelte Cypress suite against the built preview. Do not alter React product behaviour, Review Lab, production routing, deployment, database, live data, secrets, OAuth or Stage 27. Keep the diff limited to this blocker and leave the existing branch ready for fresh PR checks.",
  "mode": "implement",
  "target_branch": "migration/svelte-platform-contracts"
}
