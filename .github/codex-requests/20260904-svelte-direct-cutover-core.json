{"mode":"implement","target_branch":"migration/svelte-direct-cutover","task":"Begin the direct React-to-Svelte frontend cutover on the existing target branch. The public frontend is currently unavailable, so apps/web is the sole next deployable frontend target; do not preserve a dual-runtime deployment strategy or add new React product architecture. Read CLAUDE.md, docs/ROADMAP.md, docs/development/v3-application-stack-decision.md, docs/api-contracts.md, the current React application and all parity tests before editing. Implement the complete first vertical migration foundation, not another placeholder: (1) port the application shell/navigation/footer/skip link/error handling into SvelteKit with accessible DOM semantics and preserved data-testids; (2) implement path routing plus a client hash-redirect compatibility shim for existing #system/N, tab/system, my-work/watchlist/pinned/colony aliases, map, compare, search-tuning, fc, colony-planner nested routes, admin and operator links, while preserving FastAPI ownership of /api/*, exact /openapi.json and numeric /s/{id64}; (3) build a hand-written application-facing wrapper over the generated Hey API client that preserves credentials:'include', structured ApiError(status,path,body), X-Admin-Token injection from sessionStorage, system envelope unwrapping and request-default behaviour, and adds bigint-safe id64 response parsing/transport so no response id64 passes through Number; add focused tests; (4) establish Svelte context/runes stores and exact compatibility readers/migrations for all existing persistent contracts: localStorage ed_pinned bare-array legacy shape, ed_compare_v2, ed_sync_key, ed_selected_route, ed_my_work_v1, ed_colony_projects_v1 v3, ed_expansion_plans_v1, ed_fc_v2, ed_profile_sync_key, ed_profile_sync_last, ed-finder:selected-system-context, density key after tracing it, plus sessionStorage ed_admin_token and ed_operator_selected_source_run; do not orphan stored user data; (5) port auth/session/account controls/owner access gate; (6) port a functional Finder vertical including search form, autocomplete/ref-system picker keyboard behaviour, result cards, pinned/watchlist state, My Work aliases and cluster/region search where needed for existing journeys; preserve query keys, optimistic rollback and current API contracts; (7) remove the Svelte placeholder claims for migrated surfaces and retire the PWA/service-worker path from the future app; (8) move frontend coverage/build authority toward apps/web while leaving React files only as read-only behavioural/parity source until subsequent feature ports, not as a release target; (9) re-author relevant React unit/RTL tests with Vitest + Testing Library Svelte and expand Cypress to cover shell, hash redirects, auth gating, search, pins/storage compatibility, watchlist contract and direct refresh; (10) use Node 24/pnpm 11 and Node24-compatible immutable GitHub action SHAs; never add ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION. Keep Babylon/Stage 27 renderer implementation, production deployment, DB mutation, OAuth activation, host/secret access and unrelated cleanup out of scope. Run focused Python contracts, pnpm check/lint/format/test/build, Cypress where feasible, and git diff --check. Commit and push all coherent work to the existing branch; do not create another PR."}