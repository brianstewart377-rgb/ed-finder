{
  "mode": "implement",
  "target_branch": "migration/svelte-platform-contracts",
  "task": "Bring PR #585 branch migration/svelte-platform-contracts to a coherent exact-current-head platform baseline. Read CLAUDE.md, PR #585, issue #580, the latest checks, apps/web platform/API/persistence/auth/static-preview code and the current remote head of migration/svelte-admin-token-writer. Verify whether the dependency-free Node 24 static preview/deep-route repair is present on the remote platform branch; do not trust prior comments about unpushed SHAs. If missing or failing, implement it now with safe static resolution, frontend 200.html fallback, exact /api, /openapi.json and numeric /s/{id64} proxy ownership, GET/HEAD, query preservation, traversal/malformed encoding rejection, explicit disposable API target and graceful shutdown. Integrate the completed admin-token writer branch with real merge ancestry, or implement its narrow session-only credential bridge directly if that child branch failed: successful trimmed owner claim persists ed_admin_token only in sessionStorage; failed claim preserves prior state; explicit logout clears it; no URL/localStorage/profile-sync/log/render leak; bounded header allowlist unchanged. Reconcile imports/tests without circular authority or duplicate stores. Run deterministic pnpm install under supply-chain policy, check, lint, format check, unit tests, build and built-preview Cypress including cold direct navigation/refresh, exact proxy and malformed-path contracts; run affected Python auth/persistence/static-preview contracts and git diff check. Fix all failures without suppressing warnings. Do not alter React features, merge main/#578, deploy or access production/database/DNS/OAuth/secrets. Leave PR #585 green and ready to merge with exact integrated parent SHAs and validation results."
}
