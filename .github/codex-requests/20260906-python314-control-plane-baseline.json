{
  "task": "Make CPython 3.14 the explicit Python baseline for the ED-Finder governed Codex/control-plane execution path so no current control-plane job deliberately selects Python 3.12. This is a narrowly scoped infrastructure hardening change from current main, independent of PR #601. Audit .github/workflows/codex-laptop.yml, .github/workflows/codex-dispatch.yml, their contract tests (especially tests/test_codex_async_bridge.py), and current control-plane documentation. Change the Codex worker setup/version gate/venv assertions and messages from 3.12 to exactly 3.14. Because codex-dispatch.yml itself executes Python to validate and serialize requests, explicitly set up Python 3.14 there before those Python calls rather than relying on Ubuntu's host-default python3; update its calls consistently if needed. Preserve all existing trust boundaries, pinned actions, model contract, immutable-base/state gates, sealed artifact, ephemeral trusted-push design, request schema, timeouts, and permissions. Do not touch production or PR #601. Do not broaden into application/runtime changes in this packet. Update tests so they positively require Python 3.14 and reject regression to the old 3.12 authority. Update docs/development/chatgpt-ops-control-plane.md so the current operational authority says 3.14. Run the focused control-plane tests and any workflow/static checks that apply. Report the created branch/SHA and exact validations.",
  "mode": "implement"
}
