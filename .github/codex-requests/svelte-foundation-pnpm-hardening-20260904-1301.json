{
  "task": "Remediate the remaining Semgrep findings on PR #576, target branch frontend/svelte-foundation, exact current head 0a5d8be81343ed3105539938a196981bebd3d9a5. Keep scope strictly to pnpm workspace supply-chain hardening and a regression contract. In apps/web/pnpm-workspace.yaml preserve the existing packages and allowBuilds configuration and add exactly the policy settings required by the blocking Semgrep findings: blockExoticSubdeps: true, minimumReleaseAge: 10080, and trustPolicy: no-downgrade. Update tests/test_svelte_web_foundation_contract.py with a focused test that parses or otherwise verifies those three exact settings so they cannot silently regress. Do not change dependencies, package versions, pnpm-lock.yaml, application code, README/docs, backend/runtime/deployment, React reference behaviour, or Babylon/Stage 27 authorization. Validate from apps/web that pnpm install --frozen-lockfile succeeds, then pnpm generate:api followed by git diff --exit-code -- src/lib/api/generated, pnpm check, pnpm lint, pnpm format:check, pnpm test, pnpm build. From repo root run python -m pytest tests/test_svelte_web_foundation_contract.py tests/test_v3_application_stack_decision.py -q. Commit and push only this bounded remediation to frontend/svelte-foundation using the worker's trusted exact-head mechanism.",
  "mode": "implement",
  "target_branch": "frontend/svelte-foundation"
}
