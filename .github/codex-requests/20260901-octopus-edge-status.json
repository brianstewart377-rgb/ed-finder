{
  "mode": "implement",
  "task": "Add a narrowly scoped READ-ONLY ChatGPT ed-new Ops operation named octopus-edge-status for the MevSpace server (hostname ed-finder-prod, FQDN nb79a3d.mevnode.com). Purpose: inspect what currently owns/routs public ports 80/443 and what is needed to expose the already-running Octopus v1.0.122 app at 127.0.0.1:43300 as https://octopus.ed-finder.app. Use the existing .github/workflows/chatgpt-ed-new-ops.yml / ed-new-operator control-plane patterns and pinned ED_NEW SSH trust. Requirements: fail closed, no writes or service restarts, no DB access, no .env or secret contents, no private key contents. Report hostname; listeners for 80,443,43300; relevant Docker containers/images/port mappings; Octopus internal /api/health and /api/version status; current host-side/containerized nginx or reverse-proxy configuration relevant to server_name/proxy_pass for ed-finder.app/octopus.ed-finder.app (read only, do not dump unrelated secrets); whether DNS resolution for octopus.ed-finder.app works from the host; HTTP/HTTPS response status using Host/SNI where safely possible; and certificate-name availability only (never private key material). Emit a concise machine-readable receipt with conclusions such as dns_present, listener_80, listener_443, octopus_internal_healthy, route_present, certificate_present. Add focused tests for allowlisting/fail-closed/read-only guarantees. Do not access production yourself; only implement/test repository changes so ChatGPT can invoke it afterward."
}