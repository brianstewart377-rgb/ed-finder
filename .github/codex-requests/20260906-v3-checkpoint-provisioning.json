{"mode":"implement","task":"Continue the V3 exit path after merged PR #619. Implement the next bounded task needed to make the Contabo live-checkpoint actually deployable. Current main is 28278bbb568b4e4502b8fa944fe915b3711665e6 and deploy/v3-live-checkpoint/target-authority.json is deliberately stopped. Build a governed, auditable provisioning/control-plane path for the NON-PRODUCTION Contabo checkpoint host vmi3542235 only. Do not touch production, do not copy production data, do not change production DNS, and do not run host mutations during this implementation worker itself. Requirements: (1) provide an allowlisted request-triggered GitHub Actions operator workflow that can provision/check the Contabo checkpoint prerequisites without git pull/build on the host; it must preserve all three Codex runner services; (2) install/verify Docker Engine + Compose and psql through a bounded idempotent host bootstrap, create the persistent external app network, durable receipt paths, Docker config/context, secure API env path, and a separately owned persistent NON-PRODUCTION PostgreSQL 18 checkpoint data source; create it fresh from current repository schema/migrations and only repository-owned synthetic/public-safe fixture/reference data sufficient for first Finder/Inspect live testing—never production data or a production dump; (3) generate credentials on the target and keep them target-local with strict ownership/modes; never print or artifact secret values; (4) generate a fresh sanitized schema-identity receipt matching current sql migrations and verify it against the new DB; (5) create a bounded non-production edge route to the loopback checkpoint origin using the Contabo host/FQDN where safely possible, with no production routing; (6) prove GHCR pull authority safely; anonymous/public pull is acceptable only if actually proven, otherwise expose an explicit secret-backed authority contract without inventing credentials; (7) provisioning must emit a sanitized target-authority candidate/receipt containing only observed facts, suitable for a reviewed follow-up authority commit, rather than silently writing main; (8) add a request-triggered dispatcher that can invoke the existing canonical 'V3 application immutable release' and 'V3 application live-checkpoint deploy' workflows with exact inputs after authority is merged, so ChatGPT can carry this through without manual GitHub UI; keep canonical release/deploy workflows as the actual execution boundaries; (9) add focused fail-closed tests and documentation. Redis/Valkey and NATS are NOT checkpoint dependencies; do not provision them. Do not weaken PR #619's immutable/digest-pinned, no-production, no-build-on-host, no-migration-during-app-deploy, network-topology, receipt, or rollback safeguards. If a full public-safe Finder/Inspect dataset cannot be reconstructed from repository-supported sources, seed a clearly synthetic minimal non-production dataset sufficient to exercise Finder/Inspect and document that limitation rather than copying production. Push the implementation to a new branch and seal the result normally.","target_branch":""}