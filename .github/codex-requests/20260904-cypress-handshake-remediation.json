{
  "mode": "implement",
  "target_branch": "migration/svelte-cypress-hard-cut",
  "task": "Repair the exact first-tranche CI failures on draft PR #578, current target head 9733a419c9675387d71306aa7754bd95c0aa2a53. Work only on the existing target branch; do not open another PR and do not merge. Read CLAUDE.md and the pull-request acceptance policy first. Preserve the owner-approved hard cut: Cypress remains the only active browser framework and Playwright must not return.\n\nObserved exact-head evidence:\n- CI, coverage, CodeQL, Semgrep and container-image parity are green.\n- Cypress Chrome product specs pass; the job fails only because review-environment.cy.js is included in the ordinary **/*.cy.js glob.\n- Cypress Firefox passes auth 3/3 and release-gate 15/16. The sole product failure is the System Detail Escape-close assertion at release-gate.cy.js around line 328: `system detail modal closed by Escape: expected false to equal true`. The Review Lab collector then fails for the same env issue.\n- Review Lab focused/static tests pass (128 passed, 1 skipped), stack/preflight/teardown succeed, but the browser summary is missing because the collector fails before writing it.\n- Exact Cypress error: `Cypress.env() does not work when allowCypressEnv is set to false ... reviewLabRun`.\n\nRequired repairs:\n1. Keep `allowCypressEnv: false`. Do not weaken it and do not use `Cypress.env()` or `cy.env()` for the trusted Review Lab handshake. In `frontend/cypress.config.cjs`, register a Node-side `getReviewLabConfig` task that returns only the process-owned values derived from `EDFINDER_REVIEW_LAB_RUN`, `EDFINDER_REVIEW_OUTPUT_PATH`, and `EDFINDER_REVIEW_SCENARIOS_JSON`. Keep the existing summary-write task, but harden it: require the signed Review Lab marker, require the requested output path to equal the process-owned configured path exactly, require an absolute path under the Review Lab-owned temporary root, validate the summary is a non-array object with the expected schema version, and fail closed on mismatch. Do not expose arbitrary file-write authority to spec input.\n2. Rewrite `frontend/cypress/e2e/review-environment.cy.js` to obtain the handshake through `cy.task('getReviewLabConfig')` and carry the returned immutable config through the collector without any Cypress env API. Preserve the existing `browser-summary.json` schema, Python evaluators, scenario selection, five viewport profiles, four evidence postures, API capture, console/page-error capture, keyboard/Escape journeys, telemetry aria-expanded checks, Delta 503-to-fallback correlation, product observations and overflow <=4px evidence. Invalid or incomplete handshake must fail before browser journeys and must not write outside the trusted output path.\n3. Ordinary Cypress parity must explicitly run only `cypress/e2e/auth-owner-access.cy.js` and `cypress/e2e/release-gate.cy.js` in both Chrome and Firefox. The Review Lab collector must run only through the Python Review Lab runner with its signed handshake. Prefer an explicit `--spec` list in `.github/workflows/cypress-parity.yml` and package scripts rather than relying on broad glob exclusion. Preserve the temporary protected-check compatibility alias, backed only by the real Cypress result.\n4. Make the System Detail Escape-close test reliable in both Chrome and Firefox without weakening the product assertion. The React modal listens on the window keyboard path; dispatch a real bubbling/cancelable `KeyboardEvent('keydown', { key: 'Escape', code: 'Escape', bubbles: true, cancelable: true })` on `window` (or the verified exact listener target), then assert the modal and relevant URL/hash state close correctly. Do not skip or browser-condition the assertion. Add a focused contract/regression assertion for this cross-browser path.\n5. Update `tests/test_e2e_harness_contract.py`, `tests/test_local_review_test_environment.py`, and any narrowly affected Review Lab/Cypress tests so they lock: no Cypress env API in the collector; Node-task handshake and bounded summary write; ordinary parity spec list excludes collector; Review Lab runner explicitly selects collector; Firefox remains required; Escape is dispatched to the correct target; no Playwright invocation/dependency returns. Do not loosen the Python summary validation.\n6. Run focused Python tests, lint/format, Cypress Chrome and Firefox product specs, and the full local Review Lab verification where feasible. Commit a coherent fix through the wrapper to the existing target branch. No production, database outside disposable CI, host, secret, DNS, deployment or recovery action." 
}