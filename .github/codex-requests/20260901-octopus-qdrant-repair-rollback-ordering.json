{"task":"Implement the final hardening for the existing allowlisted operation octopus-qdrant-healthcheck-repair. Start from current main and incorporate the intended hardening changes from commit 9290c9c8 / branch codex/run-33495441093-1, but correct this safety regression: EDIT_COMMITTED must NOT become true until AFTER `docker compose --project-directory \"$OCTOPUS_DIR\" -f \"$COMPOSE_FILE\" config --quiet` succeeds. If the compose edit was made and validation fails, the EXIT trap must still restore the backup. Preserve idempotent acceptance of the exact already-fixed healthcheck, exact qdrant/web/postgres image guards, --no-deps service scope, read-only Postgres health verification, exact Octopus version 1.0.122 verification, no secrets/.env values in output, no DB/volume/migration/env mutations, and machine-readable receipt. Add or strengthen tests so the rollback ordering regression cannot recur. Do not deploy or access production. Keep changes narrowly scoped to this operation/tests/docs and fail closed.","mode":"implement"}