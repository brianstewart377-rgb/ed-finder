{
  "mode": "implement",
  "target_branch": "codex/run-34097461617-1",
  "task": "Fix only the current Semgrep failure on PR #627 exact head 7497fe9b7ccdf5413d94ac3388390bb27e62d741. Semgrep blocks scripts/operator/install_v3_checkpoint_host_interface.py at the raw http.client.HTTPSConnection('api.github.com', timeout=20) call under rule python.lang.security.audit.httpsconnection-detected.httpsconnection-detected. Preserve the already-reviewed security design: root-controlled /run staging source, exact current trusted-main SHA check, manifest verification, transactional install/rollback, narrow root-owned launcher, authenticated operation provenance, and no broad sudo. Replace only the GitHub trusted-main lookup implementation with a Semgrep-clean standard-library HTTPS approach that still verifies TLS certificates by default, keeps the host/URL fixed to api.github.com, enforces HTTP 200, caps response size at 1 MiB, and preserves existing JSON/SHA validation. Update the host-interface SHA256 manifest accordingly and any narrowly necessary tests. Do not redesign privilege boundaries, change workflows, deploy, touch production, or trigger Octopus. Run the focused privilege-interface tests and Semgrep-equivalent validation if available, then push the existing PR branch with exact-head lease." 
}
