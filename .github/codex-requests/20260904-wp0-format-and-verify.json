{
  "task": "On the exact latest tip of migration/svelte-platform-contracts, perform a strictly bounded mechanical verification pass for the current WP0 changes. Run cd apps/web && pnpm install --frozen-lockfile && pnpm format, then inspect the diff. Commit only Prettier formatting changes to the files already reported by CI (src/lib/api/events.ts, src/lib/api/events.test.ts, src/lib/components/SessionAdminTokenControl.svelte, src/lib/components/SessionAdminTokenControl.test.ts, src/lib/components/SystemOverlay.test.ts), unless Prettier itself identifies another file changed by the same unformatted WP0 commits. Do not alter behaviour, dependencies, generated client files, workflows, React code, backend code, or documentation. Then run pnpm check, pnpm lint, pnpm format:check, pnpm test, and pnpm build. If a test or type failure reveals a small direct defect in those same WP0 files, repair it with the smallest change and add/update the focused test; otherwise report the failure without broadening scope. Push the resulting commit directly to migration/svelte-platform-contracts. No production, database, deployment, recovery, secret, or live-data action.",
  "mode": "implement",
  "target_branch": "migration/svelte-platform-contracts"
}
