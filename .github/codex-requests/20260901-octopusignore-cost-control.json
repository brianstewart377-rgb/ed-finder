{
  "mode": "implement",
  "task": "Create a conservative root .octopusignore for the ED-Finder repository to reduce Octopus indexing/review noise and API spend without hiding meaningful application, infrastructure, migration, security, or test logic. Read CLAUDE.md, .gitignore, repository structure, generated/artifact directories, and Octopus .octopusignore semantics. Prefer excluding clearly generated/build/cache/coverage artifacts, large captured outputs/data fixtures that are not useful review targets, vendored/dependency content, and lockfile churn only where that is genuinely appropriate for this repo. Do NOT ignore migrations, Docker/CI/operator scripts, application source, configuration/security files, or tests broadly. Explain each non-obvious pattern with comments in .octopusignore. Add a focused test or static validation if the repo has an appropriate pattern for config-file safeguards. Do not access production or external credentials. Run relevant focused checks and end with a concise risk summary."
}