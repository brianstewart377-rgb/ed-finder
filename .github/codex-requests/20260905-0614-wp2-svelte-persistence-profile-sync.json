{
  "task": "Implement WP2, Persistence + profile-sync parity, on existing isolated child branch migration/svelte-persistence-profile-sync, created from PR #585 exact head 4410786b689781dcfabf87a77477dfffb75cb345. Read CLAUDE.md, docs/development/v3-application-stack-decision.md, apps/web/src/lib/persistence/{storage,stores,context,fixtures}.ts and tests, apps/web root layout hydration, the React source owners frontend/src/features/{profile-sync,useProfileSync.ts,my-work/myWorkStore.ts,expansion-plans/expansionPlanStore.ts,compare/useCompare.ts,fc-planner/useFcPlanner.ts,colony-planner/colonyProjectStore.ts}, frontend/src/store/{pinnedStore,syncKeyStore,selectedRouteStore}.ts, and the attached migration intent embodied by issue #580. Complete the repository-defined WP2 without feature UI or routing work. Make the Svelte persistence services the tested sole contract owners for every existing local/session key and exact legacy envelope. Preserve Id64 decimal strings and reject unsafe legacy numeric IDs rather than retaining rounded values. Add a safe normalisePlanRecord/migration path for ed_expansion_plans_v1 so corrupt/non-object records cannot crash selectors; preserve valid slot data and clear stale project links exactly as the React tests require. Preserve ed_colony_v2 as opaque profile-sync passthrough even though no active store owns it. Implement framework-neutral profile-sync gather/apply helpers and a testable command service for pull/push semantics: explicit only, merge-not-replace on pull (missing keys do not clear local state), 404 pull means empty slot, 413 remains a structured error, successful pull updates current stores without synthetic StorageEvent hacks, successful push records ed_profile_sync_last, and neither ed_admin_token nor ed_operator_selected_source_run nor ed_sync_key is ever profile-synced. Make the cross-tab decision explicit and consistent with createPersistedStore's guarded storage-event behaviour; avoid duplicate listeners or feedback loops. Preserve exact bare-array formats for ed_pinned and ed_compare_v2, collection versions for colony/my-work/expansion, FC config backfill, raw profile key/last values, density defaults, selected-system Id64, and session-only keys. Add focused tests for: corrupt expansion envelope/record recovery; uint64-max IDs; pins/compare bare-array round trips; compare and FC cross-tab updates; my-work normalisation; density default/write; profile gather allowlist and security denylist; merge-not-replace pull; ed_colony_v2 passthrough; successful pull fan-out; successful push receipt; 404 and 413 handling; no localStorage ownership for admin token. Wire only real consumers that already exist; do not invent placeholder UI. Keep PR #585's static server, auth/admin-token bridge, route skeleton and API contracts intact. Do not touch Review Lab collector, React deletion, feature UI, routing grammar, production, deployment, DB, OAuth, secrets, or Stage 27. Run apps/web check, lint, format:check and unit tests, relevant Python Svelte/persistence contract tests, git diff --check, and report exact files/tests. Leave the branch ready for a focused draft PR into migration/svelte-platform-contracts; do not merge branches.",
  "mode": "implement",
  "target_branch": "migration/svelte-persistence-profile-sync"
}
