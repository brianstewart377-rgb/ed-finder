{
  "task": "On the exact latest tip of migration/svelte-cypress-hard-cut, repair the current Review Lab collector failure with the smallest test-harness-only change. The trusted worker must attest model gpt-6-astra and xhigh reasoning under the newly merged main model contract; if that attestation or model selection fails, stop before editing. Evidence from exact-head Review Lab run 33913173763: all focused Python tests, API contracts, accessibility checks, console/network policy, stack setup and teardown passed; Alpha and Beta scenarios passed. Gamma reached warehouse-evidence-envelope-status-unknown, then technical() globally queried body, wrapped a transient [data-testid=warehouse-evidence-technical-details] element from a different evidence surface/loading state, clicked its summary, and after React rerender failed waiting for that global details selector. The failure screenshot simultaneously shows the actual planner evidence card rendered correctly using warehouse-evidence-disclosure-toggle. The fatal error was: Expected to find element [data-testid=warehouse-evidence-technical-details], but never found it. Scope technical(status, posture) to the visible [data-testid=planner-warehouse-evidence] card. Assert the requested envelope status and optional source posture inside that same card. Open technical evidence using whichever of the two supported implementations exists inside that card: compact details/summary or the EvidencePostureSummary disclosure toggle. Re-query the scoped card and disclosure after any click so a legitimate rerender or compact-to-full transition cannot leave a detached/stale subject. Fail closed if neither disclosure contract exists, and verify the final scoped disclosure is actually open (details has open or toggle has aria-expanded=true); do not merely click and continue. Do not use a global body search, first(), or an unrelated System Detail evidence card to satisfy the assertion. Preserve the still-open System Detail overlay journey, exact posture wording, report-only/canonical assertions, Delta 503-to-provenance correlation, all five viewport profiles, all four browser flows, overflow/accessibility fields, browser-summary.json schema/evaluators, and strict failure behavior. Do not change application components, evidence semantics, API/backend code, dependencies, workflows, or weaken/skip/diagnose-away any acceptance check. Add or update a focused non-networked repository contract in tests/test_local_review_test_environment.py proving technical() is rooted at planner-warehouse-evidence, does not globally search body for the disclosure, re-queries after clicking, supports and verifies both disclosure shapes, and retains status/posture assertions. Run the focused contract test, git diff --check, and the complete disposable Review Lab verification if available; also run any inexpensive relevant Cypress/static check. Push the resulting bounded commit directly to migration/svelte-cypress-hard-cut with exact-head lease. Report starting/ending SHAs, attested model/effort/run ID, root cause, files changed, checks/results, Review Lab run result, and confirm no production, database, DNS, secret, deployment, recovery or live-data action.",
  "mode": "implement",
  "target_branch": "migration/svelte-cypress-hard-cut"
}
