{
  "mode": "investigate",
  "task": "Perform a read-only V3 live-checkpoint topology inventory aimed at making the first owner-testable checkpoint deployable as soon as #601 is accepted. Do not implement, commit, push, deploy, restart services, pull images, mutate Docker state, read or write any database, inspect secret values, read private keys/tokens/passwords, change DNS, or touch production. First establish whether the self-hosted runner executing this job is the Contabo runner/checkpoint candidate host; do not assume. Record non-secret host identity evidence only: hostname/FQDN if non-sensitive, OS, CPU architecture, CPU/memory/disk capacity, Docker/Compose versions, relevant listening ports, Docker networks, container/service names and Compose project/config labels, image references/digests where already present, volume/mount PATHS and types but never secret/env file contents, and edge/nginx topology/config file paths where readable without secrets. Explicitly distinguish ED-Finder application services from Codex/Octopus/other unrelated services and identify preservation targets that a V3 checkpoint deployment must never recreate. Inspect the current #601 integration head c6787a6055345b22770a4796c39ee86da6f2ebb3 read-only (fetching git objects/refs is allowed but do not alter the working tree), especially .github/workflows/v3-application-release.yml, .github/workflows/v3-application-live-checkpoint-preflight.yml, scripts/operator/actions/v3-app-live-checkpoint-preflight.sh, docs/operations/v3-application-checkpoint-release.md, apps/api/Dockerfile.release, apps/web/Dockerfile and nginx config. Map every REQUIRED_FACT in the stopped preflight to: proven from current repository/host evidence, partially proven, or genuinely unknown. Identify the smallest safe topology-authority implementation needed to turn the stopped preflight into an executable non-production checkpoint deploy: exact app-owned service allowlist, isolated Compose project/config/bundle installation path with no target-host source checkout/build/git pull, network/API upstream/loopback/edge wiring, GHCR pull requirement without exposing credentials, non-secret config versus secret mount requirements, durable manifest/deploy-receipt/rollback-history storage, schema-identity receipt source without database reads during this investigation, origin and externally reachable smoke commands, and rollback boundary preserving PostgreSQL 18 and unrelated services. Check whether an accepted prior digest release/receipt actually exists; if none exists, say so and propose the safest bootstrap rule for the FIRST checkpoint rather than inventing a rollback artifact. Reuse trustworthy existing operator/control-plane patterns where applicable (for example exact-head leases, pinned SSH trust, immutable digests, bounded recreate allowlists, receipt formats) but do not copy production or Octopus topology blindly. Report concrete file paths and observed non-secret values, a proposed executable deployment sequence, remaining blockers classified as repository work vs host configuration/secrets vs owner decision, and a bounded implementation packet that can follow immediately after the current Python 3.14 worker without racing #601. Contabo is not production; ed-finder-prod is production and must not be accessed."
}