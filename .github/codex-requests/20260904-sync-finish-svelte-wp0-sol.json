{
  "task": "On the exact latest tip of migration/svelte-platform-contracts, complete the remaining WP0 platform tranche and synchronise it with the latest accepted migration/svelte-cypress-hard-cut head. Start by fetching both branches, recording their full SHAs, confirming a clean workspace, and merging the latest hard-cut branch into the platform branch with normal ancestry; do not rebase, force-push, or drop the newly green Review Lab/Cypress work. Resolve conflicts by preserving both the current hard-cut evidence/test authority and the platform branch's lossless Svelte contracts. Then inspect the exact branch diff, current PR #585 failures/checks, CLAUDE.md, issue #579, and the accepted Claude dossier findings. Run cd apps/web && pnpm install --frozen-lockfile, pnpm check, pnpm lint, pnpm format:check, pnpm test, pnpm build, and focused Cypress dynamic-route/platform tests through the actual built serving path. Fix all direct WP0 defects revealed by those checks, but do not start feature migration. Required outcomes: built Svelte cold routes /system/{id64} and nested /colony-planner routes return the application with HTTP 200 through the real preview/static serving contract; backend-owned /api, exact /openapi.json, and numeric /s/{id64} remain backend-owned; near-prefixes and unknown frontend routes are not stolen; legacy planner detail paths canonicalise to the overlay query without id64 rounding; every API response capable of containing id64 remains on the lossless application facade; SSE malformed frames produce an observable guarded error/reconnect state; the session-only Admin/Operator token control remains explicit and bounded; SystemOverlay has a genuine Tab/Shift+Tab focus trap plus Escape/scroll/focus restoration; URL-derived selected-system state wins deterministically over storage hydration; no second QueryClient or uninstalled persistence context returns. Add focused unit/repository/Cypress contracts for any repaired defect. Do not weaken tests, add Playwright, alter backend/database/production behaviour, invent API fields, introduce a root JS monorepo, deploy, access secrets/live data, or broaden into Finder/Planner feature UI. Format only touched files. Push the complete bounded WP0 result directly to migration/svelte-platform-contracts using the wrapper's exact-head lease. End with starting/ending SHAs, merge ancestry, files changed, checks run and exact results, and remaining risks.",
  "mode": "implement",
  "target_branch": "migration/svelte-platform-contracts"
}
