{"mode":"implement","task":"Harden the current Octopus edge topology-detail implementation before PR. Start from current main and use `codex/run-33519050766-1` / commit `7651e2c9c17a8c670e3bff8a1af251746b69a782` as the implementation reference, reproducing its read-only `octopus-edge-topology-detail` operation and then correct these review findings. 1) The version gate must be truly exact: parse `/api/version` as JSON object and accept only when `payload.get(\"version\") == \"1.0.122\"`. Reject bare strings, `release`, `tag`, `v1.0.122`, other versions, arrays, non-string versions, malformed JSON, and oversized responses. 2) Before `docker exec ... nginx -T`, verify each exact allowlisted container is running and has exact image `nginx:1.28-alpine`; fail closed on any image mismatch. The only container names remain exactly `edfinder-v3-public-auth-edge` and `edfinder-v3-proxy`. 3) The receipt field named `published_ports` must contain only actual host-published bindings (exclude exposed-but-unbound entries whose Docker inspect binding is null/empty), while retaining protocol/container port and HostIp/HostPort metadata for real bindings. Preserve the rest of the safety contract from the reference implementation: no caller parameters, exact host `ed-finder-prod`, FQDN `nb79a3d.mevnode.com`, pwd `/opt/ed-finder`, read-only Docker inspect metadata and exact-container `nginx -T` only, no Config.Env/env files/private keys/secrets/DB/arbitrary volume contents, no filesystem/service/container/network/nginx/DB/volume/migration/env mutations, bounded sanitized nginx origins/directives, structured fail-closed JSON receipt, and trusted-main ed-new workflow/dispatcher wiring. Add focused regression tests for all three corrections, especially that release/tag/bare/v-prefixed version forms are rejected and image mismatch fails before nginx exec. Do not access or deploy to production. Run focused tests and required validation and push a fresh worker branch suitable for PR."}
