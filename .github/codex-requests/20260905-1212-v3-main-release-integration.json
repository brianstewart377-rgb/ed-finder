{
  "mode": "implement",
  "target_branch": "migration/svelte-go-live-candidate",
  "task": "Continue the governed V3 integration on PR #601. Work only on the existing migration/svelte-go-live-candidate branch and preserve its history/scope. Current expected starting head is 8ee06e91cc7a91b20e7178f51454f26ef392044a. First fetch current origin/main and reconcile the target branch with current main without rebasing, resetting, force-rewriting, or dropping #601 work; #601 was observed six commits behind main at dispatch time. Resolve overlaps deliberately in favour of the current V3/Svelte authorities and keep the single-PR integration model. Then integrate the useful implementation from release/v3-checkpoint-deploy at commit 7e44f0181f0f32f4f0e353b5d01da8fcac1fe392 into #601. That source commit is the completed immutable V3 application checkpoint release/deploy-preflight foundation: digest-pinned web/API image release workflow, strict release manifest, release Dockerfiles/nginx config, operator documentation, fail-closed deploy preflight, and its tests. Do not blindly preserve stale wording or duplicate files where main/#601 has newer authority; port/resolve the implementation coherently. Preserve the key safety model: Contabo is the live-checkpoint environment, NOT production; no host-side build or git pull; releases originate from reviewed main exact SHA, produce immutable digest-pinned images/manifests, and promotion/deployment must fail closed until target topology, secrets, schema compatibility, health/smoke and rollback facts are proven. Do NOT deploy, mutate Contabo, use credentials, change DNS, mutate databases/schema, or weaken preflight gates. Preserve the just-landed Svelte/Babylon executable foundation as an explicitly non-product diagnostic boundary only; do not implement Finder/Inspect product UI or product map design in this packet. Check for conflicts between the release web Dockerfile and current apps/web Svelte/Babylon dependency/install/build model, and make the release image actually build the current apps/web package deterministically. Check API release dependency locking against current backend packaging without broad unrelated dependency churn. Run the strongest focused validation that is practical in the worker: repository state gate; git diff --check; relevant Python contract tests including tests/test_v3_application_checkpoint_release.py and affected CI/stall/operator tests; apps/web install with frozen lockfile, check/typecheck, unit tests, build, and the spatial-foundation Cypress smoke if the runner/browser supports it. Also run any repository contract tests touched by main reconciliation. If a focused test exposes a packet-related defect, fix it in this same packet. Do not spend time micro-fixing unrelated legacy Review Lab/Product E2E behaviour; report unrelated failures explicitly. End with a concise summary of main reconciliation, release/preflight files integrated, tests run/results, and remaining blocker facts required before any Contabo mutation can ever be enabled."
}
