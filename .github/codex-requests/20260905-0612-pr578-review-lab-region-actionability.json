{
  "task": "Repair the remaining exact-head Review Lab failure on existing PR #578 branch migration/svelte-cypress-hard-cut, currently at 7ecb4aeefdd7227af1f62f92565c30707671e4c6. Read CLAUDE.md, frontend/cypress/e2e/review-environment.cy.js, tests/test_local_review_test_environment.py, frontend/src/features/colony-planner/WholeSystemColonyPlanner.tsx, and the Review Lab Python evaluators. Use Review Lab run 33928637047 on descendant PR #585 as the first-hand regression evidence: all four alpha/beta/gamma/delta scenarios passed, all 38 API responses and ordered Delta 503->provenance 200 behaviour passed, console/static/teardown passed, and profile failure attribution is now correct. The sole fatal error is in currentPlannerTelemetryRegion(): Cypress says the fixed/sticky telemetry region is covered by one of its own descendant divs, so `.and('be.visible')` on the whole container fails before the actual native toggle is queried. The screenshot shows the planner and telemetry content mounted. Implement a narrow fail-closed collector repair: do not require Cypress actionability/centre-point visibility on the non-interactive telemetry region container. Retain exact current planner and region uniqueness, aria-label/layout ownership, attachment, nonzero geometry, computed display/visibility (not hidden/none), and scoped native button/panel semantics and linkage. Apply scroll/actionability only to the actual planner-telemetry-dock-toggle button; re-query it before focus and after each Enter transition; positively prove false->true->false aria-expanded, region data-mobile-dock and controlled panel data-open/linkage. Do not use force, invoke React handlers, mutate attributes, use global :visible selectors, `.first()`, or retain stale subjects. Update focused Python contract tests so they reject the old region `.and('be.visible')` requirement and instead demand non-interactive region renderability/geometry plus button-level visibility/actionability. Preserve the complete evidence disclosure repair, 5x4 matrix, browser-summary schema, failure attribution, ordered Delta fallback, overflow, Escape and passivity checks. Run tests/test_local_review_test_environment.py, git diff --check, and the full Review Lab verification if practical. Do not change product markup/CSS unless runtime evidence proves the button itself is inaccessible. Do not touch Svelte feature work, production, deployment, DB, OAuth, secrets, Stage 27, or merge branches. Leave migration/svelte-cypress-hard-cut updated and ready for exact-head checks, and report files changed/tests run.",
  "mode": "implement",
  "target_branch": "migration/svelte-cypress-hard-cut"
}
