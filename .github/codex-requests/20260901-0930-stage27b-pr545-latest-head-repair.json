{
  "mode": "implement",
  "task": "Prepare one bounded Stage 27B repair proposal from the current origin/main, preserving the complete Stage 27B candidate currently represented by PR #545 head 4fafde220505d57e01d637bc6537ab915d1edc5e while correcting exactly the seven latest Codex findings from the completed review on that head. Because the worker wrapper creates your branch from current main, first fetch origin/codex/run-33423512056-1 and materialize that PR's Stage 27B diff onto the worktree without creating intermediate commits (for example by applying the merge-base-to-PR-head diff), so the resulting single wrapper-owned implementation commit is based on current main and contains the full Stage 27B proposal plus these fixes. Do not drop the already accepted PR #529 P1/P2 contract/audit corrections. Fix exactly: (1) cpu-spatial-index picking must unproject the supplied CSS pointer through the Babylon camera and query the galaxy plane at that pointer, with off-centre regression coverage; (2) after star-origin rebasing on galaxy scene load, reapply/recompute the camera so the new fixture is framed correctly; (3) spatial-workbench E2E must accept canonical uppercase WEBGPU/WEBGL2 backend values without weakening lifecycle/picking assertions; (4) client LOD visibility culling must not set source/stream truncation telemetry; (5) fixture selection changes made during async engine initialization must survive initialization completion; (6) bufferBytes must measure the actual thin-instance GPU matrix+color payload; (7) explicit recovery must restore retained star resources through one upload path, not rebuild twice. Preserve all earlier Stage 27B fixes already present on PR #545. Run focused spatial-runtime unit tests, TypeScript typecheck, isolated workbench build/E2E if the environment supports them, lint/Knip, git diff --check, and report any honest environment limitations. HARD SCOPE: no production map cutover or production route wiring; no Stage 27C+; no V3/ratings; no Journal ingestion or Commander History product activation; no production access/writes/deploy; no CRE/CPE mechanics changes; no System Map product/infrastructure; keep Stage 26E R3F/Three.js production and rollback intact. Do not merge, deploy, access production, or start Stage 27C. Keep the diff limited to the Stage 27B candidate and these repairs."
}
