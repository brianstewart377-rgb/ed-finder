{
  "mode": "implement",
  "target_branch": "migration/svelte-parity-ops",
  "task": "Implement the complete application shell, authentication, owner/admin/operator and live operational-read surfaces for ED-Finder V3 Svelte parity on existing isolated branch migration/svelte-parity-ops. Repository work only: do not deploy, access production/live data, read secrets, alter OAuth configuration or databases. Read CLAUDE.md, issue #580, PRs #578/#585, V3 stack/auth/deployment docs, backend dual-mode auth contracts, all React shell/admin/operator code/tests/Cypress specs and current apps/web platform. Fetch and integrate the exact completed platform and session-token writer heads required by this branch with real ancestry and record SHAs. Port every live user-visible behavior with no placeholder or React embedding: responsive app shell/navigation/error/recovery boundaries, auth bootstrap/logout/Frontier return-to flow, owner gate and one-time admin-token claim, private admin/operator routes, data/cache/enrichment/status/source-run/observed-fact controls, safe read/write confirmations, session-only token lifecycle and exact endpoint allowlist, event/news/watchlist polling and SSE reconnect/fallback/status UI, cross-tab persistence and diagnostics. Preserve cookies, CSRF/auth semantics, no token in URL/localStorage/profile sync/rendered copy/logs, fail-closed network/error states and lossless identifiers. Use generated/shared API authority and structured errors. Match accessible labels, keyboard/focus/Escape/dialog behavior, mobile/desktop layouts and route/hash compatibility. Add/port Svelte unit and Cypress coverage for guest/owner/operator and failed network/token paths, header non-leakage, live-event reconnect/polling and direct cold navigation. Remove corresponding React code/tests/imports only after no live consumer remains; otherwise leave exact retirement dependency. Run deterministic apps/web install/check/lint/format/test/build, generated API drift, affected backend/auth and React transition tests, built-preview Cypress, Python repository/security contracts, secret scan and git diff check. Fix failures and dependencies without weakening checks. Leave an independently mergeable green ops-parity branch for final release-candidate integration."
}
