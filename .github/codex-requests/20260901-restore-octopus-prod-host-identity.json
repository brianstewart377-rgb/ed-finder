{"task":"Production read-only evidence from ChatGPT ed-new Ops run 22 shows the MevSpace host identity is hostname short name `ed-finder-prod` and FQDN `nb79a3d.mevnode.com`. PR #552 incorrectly changed Octopus operator host guards to `ed-finder`, causing the allowlisted `octopus-qdrant-healthcheck-repair` operation to fail closed with `unexpected_host` before any mutation. Implement a narrow correction on current main: restore the expected production short hostname to `ed-finder-prod` everywhere #552 changed it for Octopus edge-status and Qdrant-healthcheck-repair operator contracts, including relevant README/tests. Preserve all fail-closed safeguards, exact FQDN check, structured receipts, version/image checks, and the non-destructive testing principle: no test may execute the real production repair action or reach Docker, `/opt/octopus`, service recreation, DB/volume/migration/env operations. Add/adjust focused tests proving only `ed-finder-prod` is accepted and stale `ed-finder`/other hosts are rejected without invoking production actions. Do not access, deploy to, or modify production. Run focused tests and repository-required validation, then push a worker branch suitable for a replacement/follow-up PR.","mode":"implement"}
