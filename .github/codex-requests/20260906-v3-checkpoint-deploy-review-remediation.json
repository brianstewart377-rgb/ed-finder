{
  "task": "Remediate all substantive Codex Review findings on PR #619 exact head 7c38338a5dba3016156dd2ed92fc95fcfea11272 in one coherent repository-only pass on target branch checkpoint/v3-first-live-deploy. DO NOT deploy anything, access or mutate Contabo, production, databases, DNS, secrets, registry credentials, or host services. Preserve the existing immutable-release, non-production, fail-closed checkpoint design and keep Svelte/product/Search/Grid/Cluster work out of scope. Findings to resolve and verify: (1) P1 deploy/v3-live-checkpoint/compose.yml: standard API lifespan can call reap_stale_admin_operation_runs() and UPDATE admin_job_runs at startup while the deployment receipt claims no database mutation. Add a narrowly scoped, explicit checkpoint-safe mechanism that prevents this startup housekeeping write for the live checkpoint without making the whole application unusably read-only; default normal application behaviour must remain unchanged unless the checkpoint explicitly opts out. Add focused tests proving checkpoint startup does not perform this write and that ordinary runtime semantics are preserved. (2) P1 scripts/operator/v3_checkpoint_deploy.py: docker compose up --detach is followed by a single immediate smoke attempt. Add bounded readiness polling with a clear timeout/backoff before the authoritative smoke suite, covering bootstrap, upgrade and rollback paths as appropriate, and test normal startup latency/failure behaviour so transient startup cannot cause false rollback. (3) P1 scripts/operator/v3_checkpoint_deploy.py: bind the schema/migration receipt to the database actually configured by the authorized external API env file. Before any service mutation, verify a non-secret database identity and the current applied migration set against the authoritative receipt using a read-only query/path; do not print or persist DATABASE_URL credentials. Fail closed on env-file repointing, stale receipt, migration drift, or unverifiable identity. Prefer existing trusted release/runtime helpers and keep all DB access read-only. Add tests for mismatch, stale/drift and success. (4) P2 scripts/operator/v3_checkpoint_deploy.py: in upgrade mode, pre-mutation smoke_origin reaches /api/health and SELECT 1, but failure receipts can incorrectly say no DB access when a later pull/verification fails. Track database access independently of service mutation and make receipts accurate on all failure/success paths; tests required. (5) P2 .github/workflows/v3-application-live-checkpoint-preflight.yml: 15-minute job timeout is shorter than the deployer's own worst-case bounded candidate+rollback pull/inspect operations. Set a defensible workflow ceiling above the full valid deploy/readiness/smoke/rollback/receipt budget while preserving bounded per-operation timeouts; add/update contract tests. (6) P1 workflow/deployer rollback source: upgrade currently depends on downloading the prior accepted release manifest from a GitHub Actions artifact retained only 90 days. Remove that expiry dependency by durably persisting each successfully accepted release manifest with the checkpoint's host-side deployment receipt/history (or an equivalently authenticated non-expiring source) and use that durable accepted prior manifest for later rollback eligibility. Preserve provenance/digest verification and fail closed if durable rollback state is missing/corrupt; do not allow regeneration from a different SHA. Add upgrade tests beyond artifact expiry semantics and update operator docs. Also re-run the focused checkpoint/release tests, relevant API startup tests, repository contracts, lint/type checks needed by the touched files, and git diff --check. Keep the existing resolved CodeQL false-positive disposition untouched. Deliver one sealed implementation commit and push it with the governed trusted-push path to the existing target branch; do not merge PR #619.",
  "mode": "implement",
  "target_branch": "checkpoint/v3-first-live-deploy"
}
