{
  "mode": "investigate",
  "task": "READ-ONLY CONTABO CAPACITY DIAGNOSTIC. Do not modify the repository, do not access production, do not print environment variables/tokens/credentials, and do not stop/restart any service or runner. This run is solely to answer whether the Contabo host that carries the three self-hosted Codex runners has enough spare capacity for a small isolated ED-Finder V3 checkpoint stack without degrading runner reliability. Report the runner name and hostname; whether the runner is directly on the host or inside a container/VM; nproc and concise lscpu CPU topology; free -h plus MemTotal/MemAvailable/SwapTotal/SwapFree; uptime/load averages; cgroup v1/v2 CPU and memory limits/current usage when visible; /proc/pressure cpu/memory/io when visible; df -h and df -i for /, /tmp and the GitHub workspace filesystem; Docker availability and, only if safely readable without privilege, a sanitised summary of container count and aggregate docker disk usage (do not inspect env, mounts, secrets, or application data); top CPU/memory processes with command lines sanitised; recent OOM/memory-pressure evidence only if readable without privilege. Explicitly distinguish HOST-WIDE values from per-runner/container-limited values. Then assess capacity for a checkpoint consisting approximately of nginx/static web + FastAPI + one small PostgreSQL + Valkey, with conservative CPU/RAM/disk limits, while preserving headroom for all three Codex runners to run concurrently. Give a recommended maximum checkpoint resource budget and a YES / YES-WITH-LIMITS / NO conclusion. If host-wide visibility is insufficient, say so and give the exact missing evidence rather than guessing. No writes, no deployment, no DB access, no network probing beyond local host metadata."
}
