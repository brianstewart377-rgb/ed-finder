{
  "mode": "implement",
  "target_branch": "migration/svelte-go-live-candidate",
  "task": "Continue the governed V3 integration on PR #601. Work only on the existing migration/svelte-go-live-candidate branch and preserve its history/scope; expected starting head is cce525da404c1453a6dc44d520026feff86a7ac8 and the wrapper's exact-head lease is authoritative. This is ONE consolidation/stabilisation packet before Finder/Inspect product work. Do not deploy, mutate Contabo, use live credentials, change DNS, mutate databases/schema, or implement Finder/Inspect product UI. Contabo is a live-checkpoint environment, NOT production.\n\nFIRST: audit and actually complete the consolidation that #601's operating model requires. Current #601 does not yet contain all load-bearing WP0/WP1/WP2 implementation. Treat these as source evidence, not blind merge authority: PR #585 / migration/svelte-platform-contracts head e370ee97838f04ad89d25af70574bd4eeaf524cd (latest WP0 platform contracts); PR #594 / migration/svelte-shared-core head fc3b5fccad2270bee75766cc3420df0bd7df4ba4 (WP1 shared packages); PR #599 / migration/svelte-review-evidence head 072be9bab409e48f3f064900e48c7ef6f724d7aa (prior WP0+WP1 integration proof); PR #600 / migration/svelte-persistence-profile-sync head 408721c6a59ab38ee41eb416822ae40bceaf9492 (WP2 persistence/profile sync). Compare all four against current #601 and port/merge the unique load-bearing contracts without resurrecting stale architecture or dropping newer #601 work. Preserve useful Git ancestry where practical, but correctness and current V3 authority win over mechanical merges.\n\nRequired consolidation outcomes: canonical/lossless Id64 handling above 2^53 and through uint64 max; same-origin typed API facade/transport with cookie credentials, structured errors and bounded session-only admin-token handling; SSE/query client contracts; auth/session/owner boundaries; persistence codecs/stores and profile-sync merge-not-replace semantics including opaque legacy data preservation where required and exclusion of session-only security keys; useful routing/hash compatibility and overlay lifecycle contracts; packages/planner-core and packages/api-client plus required workspace/package wiring and tests; no duplicate competing implementations. Preserve the current V3 canonical journey Explore -> Inspect -> Plan -> Review/Export and the current fresh-Babylon architecture. Do NOT blindly replace it with stale catch-all /finder/system routing or old UI. Compatibility redirects may be retained only if they do not become the canonical V3 route model. Preserve current apps/web Babylon 9.23.0 foundation and renderer-neutral spatial contracts/control-plane/browser-lane docs. React remains migration evidence only.\n\nSECOND: fix all currently known exact-head cce525d gate failures in this same batch. (1) tests/test_local_review_test_environment.py::test_review_lab_workflow_invokes_wrapper_authority_and_not_normal_e2e is brittle because it searches the old display labels 'Run strict resolver' and 'Run focused Review Lab and safety tests' after the workflow was intentionally renamed to migration-debt labels. Fix the contract to prove semantic command presence/order without depending on obsolete step display names; do not weaken Review Lab containment/authority and do not collapse Review Lab into normal Product E2E. This single stale assertion currently causes Review Lab, Backend unit tests, and Backend Coverage to fail; backend coverage itself is 78% and Product E2E is green. (2) Semgrep currently has two blockers from the newly integrated release foundation: apps/api/pyproject.toml lacks uv's dependency-cooldown policy, while the release Dockerfile pins uv 0.8.22; reconcile this deliberately with the locked V3 Python 3.14 + uv target. Do not silently revert the V3 release target to Python 3.12 merely to avoid the finding. Use a reviewed pinned uv version that supports the required exclude-newer/dependency-cooldown mechanism, add the fail-closed config, regenerate/validate uv.lock as necessary, and avoid unrelated dependency churn. (3) scripts/release/v3_release_run.py triggers dynamic urllib SSRF auditing. Preserve its existing strong provenance checks (canonical repo, official .github/workflows/v3-application-release.yml, workflow_dispatch, successful completed run, main head, manifest SHA equality), but replace/harden the network call so the HTTP authority is unambiguously fixed to HTTPS api.github.com and no dynamic scheme/host/file URL is possible. Prefer an implementation that satisfies Semgrep without a hand-wave suppression. Keep response bounds/timeouts/token redaction and add/adjust focused tests for hostile repository/run inputs and provenance failures.\n\nTHIRD: validate the Python 3.14 release target rather than assuming normal Python 3.12 CI proves it. The locked V3 target is CPython 3.14 + uv, while much existing migration/reference CI still runs Python 3.12. Preserve that distinction in docs/tests. Ensure the release Dockerfile/uv lock can actually build and import/start the current API under Python 3.14 without changing application behavior. Add the narrowest practical release-image or Python-3.14 compatibility check/contract so this target cannot silently rot. Do not migrate unrelated local/test tooling to 3.14 in this packet unless required by the locked release target.\n\nFOURTH: keep the immutable release safety model intact: main exact SHA only, digest-pinned images/manifests, no host-side build/git pull, release-run provenance verification, schema compatibility/rollback fail-closed, and deploy preflight remains read-only/stopped. Do not enable Contabo mutation. If consolidation conflicts with release files, resolve for the current apps/web Node24/pnpm11/Babylon build and current API package layout.\n\nValidation: run repository state gate and git diff --check; apps/web pnpm frozen install, check, lint, format:check, unit tests, build; package builds/tests for packages/api-client and packages/planner-core; focused WP0/WP2 persistence/auth/API/routing contracts; focused Python tests including tests/test_local_review_test_environment.py, tests/test_browser_validation_lane_contract.py, tests/test_v3_application_checkpoint_release.py, tests/test_v3_application_stack_decision.py, tests/test_v3_coordination_control_plane.py and affected CI/worker contracts; Ruff; Semgrep if available. Run a Python 3.14/release-image compatibility proof appropriate to the implementation. Do not spend this packet designing Finder/Inspect or retargeting the full Review Lab browser collector to apps/web yet.\n\nAt the end report: exactly which WP0/WP1/WP2 contracts were newly absorbed, any deliberately rejected stale pieces and why, tests run/results, Python 3.14 release proof, and any remaining blockers before #585/#594/#599/#600 can be closed as superseded. Do not close those PRs yourself unless equivalence is demonstrably complete and the task infrastructure explicitly allows it; report closure readiness instead."
}