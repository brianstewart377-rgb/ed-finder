{
  "mode": "implement",
  "task": "Repair the PostgreSQL 18 production-candidate promotion work from PR #524 before merge. Start from current origin/main as the wrapper requires. Fetch and inspect origin/codex/run-33316833025-1 / PR #524 as the implementation to port, including its current-head test fix d26f796, then produce a corrected implementation branch from current main. Address EVERY current unresolved Codex Review finding on PR #524, verifying each against the actual repository rather than blindly accepting it: (1) readiness coverage must compare eligible system IDs/anti-joins rather than aggregate derived-table counts so stale rows cannot mask missing eligible rows; (2) grid readiness must validate stored grid/macro-grid assignments against current coordinates and corresponding grid relations, not nullness alone; (3) required materialized-view readiness must establish refresh ordering/freshness after source builds using an existing trustworthy watermark/metadata if available, or fail closed/document the exact safe mechanism rather than treating relispopulated as freshness; (4) validate every required external DB role URL and prove they target the same intended PostgreSQL 18 server/database identity before migrations/deploy, not only DATABASE_READONLY_URL; (5) external DB deploy mode must explicitly ensure Redis is started/healthy while still preventing bundled PostgreSQL from starting; (6) readiness audit must honor the same validated MIGRATION_LEDGER_TABLE configuration as apply_migrations.sh; (7) deploy_main.sh must preserve operator-exported shell overrides when loading .env instead of allowing .env to overwrite them; (8) the GitHub readiness workflow must never upload a receipt that failed credential/secret-leak validation; (9) pin/verify the expected SSH host key/fingerprint from trusted GitHub environment/secret configuration before transmitting CANDIDATE_DATABASE_URL; unauthenticated ssh-keyscan must not establish trust for the secret-bearing connection. Preserve the existing fail-closed/read-only/secret-safe intent, PG18 exact gate, external-mode no-bundled-PG guarantee, runbook dependency order, and existing tests. Add/adjust regression tests for each corrected defect where practical. Do not deploy, access production, use production credentials, change DNS, merge, or weaken safety gates. Run focused tests plus the broadest practical affected test/lint/shell/compose checks and git diff --check. End with exact files changed, exact validation results, and any owner-only configuration still required (for example the trusted SSH host key setting)."
}
