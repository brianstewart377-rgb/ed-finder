{
  "task": "Update existing PR #628 branch codex/run-34202782475-1 to address the remaining live Codex review findings on exact current head 09cabf2fd2da1f9f08ee503a289db02346a20c0e. Keep the production authority fail-closed and do not execute any production operation. Fix all four items in one coherent hardening pass with focused regression tests: (1) scripts/operator/v3_production_deploy.py receipt persistence: if current.json has been replaced and any later pointer/fsync/persistence step fails, preserve and restore the prior current.json pointer atomically (or remove it if none existed) so rollback never leaves a pointer referencing deleted candidate receipt/manifest files; test post-replace failure. (2) workflow-driven preflight receipt accuracy: preflight creates/extracts/removes an ephemeral operation bundle on the production host, so receipts must not claim filesystem_writes_performed=false; represent this accurately without implying application/data mutation, update fallback receipts/docs/tests consistently. (3) scripts/operator/v3_production_inventory.py Docker context integrity: every Docker command used for inventory evidence (ps, inspect, network, exec/ledger, etc.) must be pinned to the inspected local default context (e.g. --context default or equivalent), and fail closed if that context is not the expected local endpoint; do not allow DOCKER_CONTEXT/current-context drift; add regression coverage. (4) scripts/operator/v3_production_deploy.py audit-state tracking: once the production API env file has actually been read successfully, any later failure receipt must truthfully report env_files_read=true; route later candidate/container/origin validation failures through receipt state that preserves this fact; never print secret values; add focused failure-path tests. Re-run the targeted production deployment/inventory tests plus relevant workflow/contract tests and any formatter/linter needed. Do not weaken acceptance tests, do not activate deploy/v3-production/target-authority.json, do not install anything or touch production, and do not change unrelated architecture. Commit the fixes on the existing branch so fresh CI and Codex review retrigger.",
  "mode": "implement",
  "target_branch": "codex/run-34202782475-1"
}
