{
  "task": "Repair the next exact-head Review Lab telemetry failure on existing PR #578 branch migration/svelte-cypress-hard-cut, currently at ff9ea52b40bf85fe0eb17a94741f9e24340b7123. Read CLAUDE.md, frontend/cypress/e2e/review-environment.cy.js, tests/test_local_review_test_environment.py, frontend/src/features/colony-planner/WholeSystemColonyPlanner.tsx, AccountControls.tsx/nav layout, and the Review Lab Python evaluators. Use first-hand evidence from Review Lab run 33949838549 and its artifacts: all alpha/beta/gamma/delta scenarios passed; all 38 API responses, ordered Delta warehouse 503 -> provenance 200 behaviour, console policy, static checks and teardown passed; currentPlannerTelemetryRegion now passes uniqueness, ownership, attachment, geometry and computed-style checks. The sole fatal error is later in exposeCurrentPlannerTelemetryToggle(): after direct `.scrollIntoView()` Cypress reports the native planner-telemetry-dock-toggle as fixed/sticky and covered by the navbar's `[data-testid=\"frontier-sign-in\"]`, so the button-level `.should('be.visible')` correctly refuses action. The screenshot/video show the page left scrolled around the Delta evidence surface and the sticky nav/header mounted. Implement a narrow deterministic collector repair based on actual layout state. Before actionability checks, reset the document/window scrolling element to a known safe position (normally page top) and positively assert that reset; re-query the current planner, telemetry region and native toggle after the scroll. Do not merely call scrollIntoView in a way that re-pins the sticky control beneath fixed navigation. If an internal telemetry-region scroll reset is also required, scope and assert it without treating the region itself as an action target. Prove the actual button has nonzero geometry and is the topmost hit target at an interior point (or that any topmost descendant belongs to that button) before focusing it. Then retain the native keyboard Enter path and fresh re-queries proving aria-expanded false->true->false, region data-mobile-dock, controlled panel linkage/data-open and focus ownership. No `{force:true}`, `.click()`, `.invoke()`, React handler calls, CSS/attribute mutation, global `:visible`, `.first()`, sleeps, or suppression of the Frontier sign-in control. Do not change product markup/CSS unless runtime evidence proves a real user accessibility defect; this failure currently indicates collector scroll positioning. Update focused Python contract tests to reject the old direct sticky-button `.scrollIntoView()`-only strategy, require deterministic document scroll reset before button actionability, require post-scroll re-query and hit-target proof, and preserve failed-profile attribution. Keep the complete 5x4 matrix, evidence disclosure repairs, browser-summary schema, passivity, overflow and Escape contracts. Run tests/test_local_review_test_environment.py, git diff --check and full Review Lab verification if practical. Do not touch Svelte feature work, WP1/WP2, production, deployment, DB, OAuth, secrets or Stage 27. Update only migration/svelte-cypress-hard-cut, leave it ready for fresh exact-head checks, and report exact files/tests.",
  "mode": "implement",
  "target_branch": "migration/svelte-cypress-hard-cut"
}
