{
  "task": "Repair the exact-head Svelte web check failure on draft PR #594 branch migration/svelte-shared-core, currently at 1b8f23976f9d1f06feb46e3b90d619d0ac21a883. Read CLAUDE.md, packages/planner-core/src/colonyProjectTypes.ts and colonyProjectTypes.test.ts, packages/planner-core/src/colonyRoles.ts and its tests/importers, the original React colonyProjectStore.ts/useWorkspaceProjectState.ts contract, and apps/web/vitest.shared.config.ts. CI run 33949342140 failed only in the shared package test `matches project snapshots including declared roles and normalised placements`: projectMatchesSnapshot returned false for semantically identical roles lacking created_at/updated_at. The cause is load-bearing: normaliseDeclaredRoles supplies new Date().toISOString() for missing legacy timestamps, and projectMatchesSnapshot normalises the saved and current arrays independently before JSON comparison, so volatile generated metadata can differ by a millisecond. Fix the production semantic comparison, not the expected test. Define a deterministic role snapshot/equality representation that preserves the fields relevant to an editable plan (identity/body/role/source/label/confidence as appropriate) while excluding persistence metadata timestamps that should not mark a plan dirty. Preserve deduplication and role validation; do not make all role changes compare equal, reorder user data silently, or weaken placement/name/notes/archetype comparisons. Add a focused regression proving semantically identical legacy roles remain equal even when normalisation calls occur at different clock times, and proving a changed body_id/role_id/confidence or relevant editable field remains unequal. Keep the implementation framework-neutral and ensure the React importer continues to use the shared source. Run shared:check, shared:lint, shared:test under the Node 24/pnpm repository harness, package builds, relevant React colony-project/workspace tests, apps/web checks if affected, focused Python package-path contracts, and git diff --check. Do not touch Review Lab, persistence WP2, feature UI, routing, production, deployment, DB, OAuth, secrets, or Stage 27. Update only migration/svelte-shared-core and leave PR #594 ready for fresh checks; do not merge.",
  "mode": "implement",
  "target_branch": "migration/svelte-shared-core"
}
