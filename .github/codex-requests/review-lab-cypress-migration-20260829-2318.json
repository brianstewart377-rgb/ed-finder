{
  "mode": "implement",
  "task": "Port Review Lab's full browser verification from Playwright to Cypress so Playwright can be removed from ED-Finder. Read CLAUDE.md and relevant Review Lab/Cypress docs first. Scope this to test/CI/review machinery only: do not change product behavior, deploy, access production, or use production credentials. Preserve the existing Review Lab semantics and structured browser-summary handshake exactly enough that scripts/dev/review_lab/browser_runner.py and its evaluators still enforce viewport-profile journeys, accessibility keyboard checks, console/page errors, API-response diagnostics, delta fallback correlation, product observations, strict failure behavior, Docker isolation/teardown, and scenario selection. Migrate frontend/e2e/review-environment.spec.js (and any helpers it needs) to Cypress or an equivalent Cypress-driven collector; update browser_runner.py to invoke Cypress; update .github/workflows/review-lab.yml so it no longer installs Playwright; update frontend package scripts/dependencies/lockfile; remove frontend/playwright.config.ts, frontend/e2e Playwright specs/helpers, @playwright/test and @axe-core/playwright only when no remaining runtime/test/review dependency needs them. Cypress must remain retries=0 for authoritative release-gate behavior. Review Lab must remain strict/fail-closed. Add/update focused regression contracts so generic CI and Review Lab cannot silently reintroduce Playwright. Spread independent investigation/porting/test work across parallel agents where appropriate for speed. Run focused Python tests, frontend typecheck/build/tests, Cypress Review Lab verification or the closest deterministic local equivalent, plus grep/contract evidence showing whether any Playwright references remain. If full Playwright removal cannot be completed safely in this pass, implement the largest safe bounded slice and explicitly list the remaining references/blockers rather than weakening Review Lab. Also package the final changed files plus a short test-evidence README into artifacts/codex-output/review-lab-cypress-migration.zip and report its SHA-256; this ZIP is implementation-output evidence and can be removed before merge."
}
