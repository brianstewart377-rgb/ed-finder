{
  "task": "On the exact latest tip of migration/svelte-cypress-hard-cut, repair one Review Lab asynchronous race in frontend/cypress/e2e/review-environment.cy.js. The exact-head artifact shows Gamma satisfying warehouse-evidence-envelope-status-unknown from the initial placeholder, technical() choosing the compact <details>, and the component switching to the dedicated EvidencePostureSummary disclosure after its evidence response settles. In planner(system), register a Cypress alias for GET /api/colony-planner/system/${system.id64}/warehouse-planner-evidence before opening the planner. After colony-planner-workspace is visible, wait for that exact response before returning. Assert its status is 200 or 503. When it is 503 (Review Delta only), wait for the exact GET /api/colony-planner/system/${system.id64}/provenance-cockpit fallback response and require status 200. Keep the existing catch-all API instrumentation, keyboard-default-action repair, technical() disclosure logic, Python browser-summary schema/evaluators, application code, backend, dependencies, workflows, and all product wording unchanged. Add or update a focused non-networked repository contract proving the evidence aliases are registered before planner activation and consumed after workspace entry, with 503 -> provenance wait ordering. Run the focused contract, git diff --check, and inexpensive relevant static checks. Push only this scoped harness/test change directly to migration/svelte-cypress-hard-cut with exact-head lease. No production, database, deployment, recovery, secret, or live-data action.",
  "mode": "implement",
  "target_branch": "migration/svelte-cypress-hard-cut"
}
