{"mode":"implement","task":"Make one narrow repair on current main for the two surviving unresolved PR #517 Codex P2 findings only. Do not deploy or access production. (1) Fix OAuth login/callback rate-limit client identification for the Cloudflare Flexible SSL path: current config/nginx.conf forwards $remote_addr as X-Real-IP, which is a Cloudflare edge address, while apps/api/src/routers/auth.py trusts nginx's X-Real-IP from the private proxy peer. Implement a secure fail-closed way to preserve the actual visitor IP without trusting spoofable client headers from direct-origin traffic; respect the existing proxy/network trust model and add focused regression coverage. (2) Fix web_sessions cleanup scalability: current OAuth callback runs DELETE FROM web_sessions WHERE expires_at <= NOW() OR revoked_at IS NOT NULL, while the existing index is only (user_id, expires_at DESC) WHERE revoked_at IS NULL. Make cleanup index-supported (for example by suitable migration/indexes and/or splitting cleanup predicates) using the next valid migration number in the repository, and add focused tests/contract coverage. Preserve existing Frontier OAuth/PKCE/session/owner semantics, ADMIN_TOKEN compatibility, operator/admin protection, migration discipline, and unrelated behavior. Run the focused auth/migration tests plus relevant lint/type/CI-contract checks available in the worker, git diff --check, and report exact files/results. Push only the bounded implementation branch; do not merge."}