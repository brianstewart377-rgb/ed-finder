{
  "mode": "implement",
  "task": "Fix only the exact-head Python 3.14 CI regression now present on PR #601 after commit aa48ff620152d7b85e5ab12ea9fb386509896367. The failing evidence is Backend unit tests + compose validate run 34028868242 job 101474640127 and Backend Code Coverage run 34028868230 job 101474640072. In both cases the V3 CPython 3.14 uv environment is created successfully and 2,127 backend tests pass; the single failure is tests/test_deploy_main_invariants_gate.py::test_invariant_wrapper_still_accepts_the_bounded_waiver_flags because scripts/run_data_invariants_receipted.sh resolves system python3 inside the subprocess PATH, which cannot import psycopg2 from apps/api/.venv. This is repository-tooling/test interpreter plumbing, not an application/runtime incompatibility. Make the smallest cross-platform fail-closed correction so this parser/receipt contract test deliberately exercises the wrapper with the same Python interpreter/environment as the pytest process (or an equivalently explicit bounded mechanism) without broadening production behaviour, weakening assertions, changing the V3 runtime target, reintroducing requirements.txt dependency resolution, touching Product E2E/Review Lab semantics, changing deployment topology, or editing unrelated legacy code. Prefer a focused test/harness correction over changing production/operator wrapper semantics unless repository evidence proves otherwise. Preserve the wrapper's real production-safe behaviour and its bounded waiver/receipt contract. Validate the exact failing test under the CPython 3.14 frozen apps/api uv environment, then the relevant backend unit suite and coverage path or their closest deterministic local equivalents. Add/update a regression test only if needed. Commit the bounded fix and use the governed trusted-push path to fast-forward migration/svelte-go-live-candidate only if its expected parent remains aa48ff620152d7b85e5ab12ea9fb386509896367; fail closed on head drift. Report changed files, root cause, validations, and pushed SHA. Do not touch production. Contabo is not production."
}