{
  "task": "Implement WP1 from the accepted planner/simulation migration dossier on the exact latest tip of migration/svelte-planner-core, which was created from the green migration/svelte-cypress-hard-cut head. Read CLAUDE.md, docs/ROADMAP.md, docs/development/v3-application-stack-decision.md, issue #580, PR #578, the current apps/web platform contracts, and all relevant React planner/simulation/evidence source plus tests before editing. This packet is framework-neutral planner-core extraction only: no Svelte feature UI and no React deletion. Create canonical Svelte-owned pure modules under apps/web/src/lib/domain/planner (and apps/web/src/lib/domain/evidence only where the logic is genuinely evidence-specific), without creating a root JavaScript monorepo or new package workspace. Extract/reimplement with minimal semantic change the coherent pure core needed by later state machines: lossless planner/system/body identifier types; load-bearing BodyPlannerLane/topology/placement types currently exported from mounted or dead React components; placement normalization, resequencing and equality helpers; simulation input/result fingerprints and stale-result predicates; project/plan normalizers and immutable snapshot helpers; structure selection/filter/eligibility rules; validation projections and validation fingerprints; whole-system comparison calculations; planning economy/topology utilities; passive evidence-language/status mapping used by planner validation; and deterministic export serializers only where they are pure and already accepted. Preserve distinct comparison engines when they have different responsibilities. Do not port component hooks, React stores, effects, DOM code, API calls, persistence, routing, renderer mutation, or UI state. Treat id64 as canonical lossless decimal strings at every new browser-domain boundary; do not copy unsafe number types. Preserve evidence passivity: evidence/provenance/simulation/optimiser/renderer output must not mutate canonical plans. Port the strongest existing pure Vitest assertions into apps/web alongside each module, add differential fixture tests where practical that exercise the same accepted inputs/outputs as the React reference, and add repository contracts proving apps/web pure domain modules import no React, Svelte component, DOM, store, API, persistence, renderer, or Node-only runtime dependency. If a useful type is currently exported only from a dead/unmounted React component, relocate it into the new pure domain module and document the eventual React deletion dependency, but keep the current React consumer compiling in this packet. Do not invent game mechanics or API/database fields. Run pnpm install --frozen-lockfile, pnpm check, pnpm lint, pnpm format:check, pnpm test, pnpm build, focused existing React pure tests that serve as parity oracles, and relevant Python repository contracts. Keep the diff reviewable; if a candidate module is not actually pure or its accepted behaviour is ambiguous, leave it out and report it rather than forcing it into this packet. Push the complete result directly to migration/svelte-planner-core through the exact-head lease. End with an inventory of modules extracted, source/parity tests mapped, commands/results, deliberately deferred modules and risks. No production, database, deployment, DNS, secret, recovery or live-data action.",
  "mode": "implement",
  "target_branch": "migration/svelte-planner-core"
}
