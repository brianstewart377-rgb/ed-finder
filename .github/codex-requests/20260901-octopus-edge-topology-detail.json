{
  "mode": "implement",
  "task": "Add a narrowly scoped read-only ChatGPT ed-new Ops operation named octopus-edge-topology-detail for the MevSpace production host ed-finder-prod. Context: Octopus v1.0.122 is healthy on 127.0.0.1:43300. The merged octopus-edge-status audit proved DNS and served TLS are valid but neither live nginx container edfinder-v3-public-auth-edge nor edfinder-v3-proxy currently contains an octopus.ed-finder.app route. We need exact live topology before installing the route. Implement only a read-only inspection operation through .github/workflows/chatgpt-ed-new-ops.yml and scripts/operator/actions/dispatch.sh using existing fail-closed patterns. The dedicated script should accept no caller parameters, verify host identity and /opt/ed-finder working directory, inspect exactly the two expected containers, and emit one concise machine-readable JSON receipt with: each container image, network mode/network names, published ports, bind/volume mounts (source,destination,type,rw), nginx -T configuration file origin paths, and a sanitized list of server_name/proxy_pass/listen/include directives sufficient to identify which host path/config file must be changed. Also report whether 127.0.0.1:43300 /api/health and /api/version return success and exact version 1.0.122. Do not read env files, private keys, databases, certificate private keys, or arbitrary file contents. Do not modify files, containers, networks, services, or nginx. Fail closed on missing/unexpected containers or nginx inspection failure. Add focused tests for allowlisting, exact container names, read-only flags, no secret/env/DB access, and structured output. Read current main and the existing octopus-edge-status implementation/topology follow-up first. Do not access production; only implement and test the repository change so ChatGPT can merge and run it through the existing MevSpace control plane."
}