{
  "task": "Fix the exact-head CI/Cypress install failure on PR #576. Target branch frontend/svelte-foundation, exact current head 93bf7bb01c96be6d5f2b2b4d2e9d2faaac2e8b24. The new pnpm minimumReleaseAge: 10080 lockfile verification rejects 38 already-reviewed packages in the checked-in bootstrap lockfile because those exact versions were published within the last 7 days. Preserve minimumReleaseAge: 10080, trustPolicy: no-downgrade, blockExoticSubdeps: true, and allowBuilds; do NOT set trustLockfile true and do NOT relax minimumReleaseAge or minimumReleaseAgeStrict. Use pnpm 11's supported minimumReleaseAgeExclude with exact package@version selectors only (not broad package-name or scope wildcards) to grandfather the complete currently locked set that fails the age policy. Derive the complete failing set from a clean/fresh frozen install or registry metadata rather than guessing from the abbreviated CI log. Future versions must remain subject to the 7-day policy. Add/update tests/test_svelte_web_foundation_contract.py so it requires minimumReleaseAgeExclude to exist, rejects wildcard/bare-package exclusions, and verifies each exception is version-qualified; keep the existing tests for minimumReleaseAge/trustPolicy/blockExoticSubdeps. Validate in a clean state with Node 24 and pnpm 11.25.0: remove any apps/web node_modules/store state that could mask verification, run pnpm install --frozen-lockfile and require success, then pnpm generate:api; git diff --exit-code -- src/lib/api/generated; pnpm check; pnpm lint; pnpm format:check; pnpm test; pnpm build. From repo root run python -m pytest tests/test_svelte_web_foundation_contract.py tests/test_v3_application_stack_decision.py -q. Do not change package.json versions, pnpm-lock.yaml, application code, README/docs, CI workflow logic, backend/runtime/deployment, React reference behavior, or Babylon/Stage 27 authorization. Commit and push only the workspace-policy exception list plus focused regression contract to frontend/svelte-foundation using the worker's trusted exact-head mechanism.",
  "mode": "implement",
  "target_branch": "frontend/svelte-foundation"
}
