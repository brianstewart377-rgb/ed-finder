{
  "task": "Update only the exact current tip of migration/svelte-cypress-hard-cut, expected at 3d4a585f756f2e686250f4e60f9a69ba93ea76ae. Preserve the just-landed warehouse-evidence scoping/re-query fix: the latest browser summary proves Alpha, Beta, Gamma, and Delta all pass, including Delta's warehouse-evidence 503 and provenance 200. The sole remaining failure in Review Lab run 33921467545 is now the first viewport profile: after the four scenarios, telemetry() times out finding [data-testid=planner-telemetry-dock-toggle]:visible at 1440x900. browser-summary fatalError points to telemetry at review-environment.cy.js around line 300; profileResults remains empty because planner_desktop_primary never completes. The failure screenshot shows the Delta planner at #colony-planner/system/7200000000004/project/... with the evidence card open and no visible plan-details dock. Diagnose this exact state using the uploaded failure evidence, current React DOM/source, and the former Playwright Review Lab oracle. Do not assume the selector is merely slow. Determine whether the control is absent, hidden, off-layout, or whether the Cypress journey is relying on an unstable terminal scenario state. Repair the Cypress collector so the desktop-primary and laptop-minimum profiles prove the real keyboard-operable telemetry control in a deterministic planner state. It is acceptable to navigate/re-open a known Alpha planner specifically for the telemetry profile check if that is the true stable parity journey; it is not acceptable to fabricate the flag, skip the assertion, remove the profile, use a hidden element, replace keyboard activation with .click(), alter product code solely for the test, or loosen the Python evaluator/schema. Preserve all five viewport profiles, all four evidence postures, exact API correlation, the newly scoped evidence disclosure helper, overflow checks, accessibility checks, and safe teardown. Add or strengthen a focused non-networked repository contract that locks the deterministic telemetry setup and native keyboard activation before the accessibility flag is recorded. Run focused pytest, formatter/lint checks, and full disposable Review Lab verification. Inspect browser-summary.json and require: all four scenarios passed; planner_desktop_primary passed; planner_laptop_minimum passed; all five profileResults present; plannerDesktopTelemetryToggleKeyboardWorks=true; delta_503_fallback_correlation_verified=true; no unexpected API/console/page errors; teardown clean. Push through the wrapper with exact-head lease. No production, deployment, live database, DNS, OAuth, secret, recovery, or live-data access.",
  "mode": "implement",
  "target_branch": "migration/svelte-cypress-hard-cut"
}
