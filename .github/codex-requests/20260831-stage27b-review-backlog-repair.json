{
  "task": "Perform a final narrow Stage 27B correctness repair on current main using the completed Stage 27B proposal branch codex/run-33421578286-1 (head 518757febf6da33e07b1e59d58a285ef5bba5a4a) as the implementation to preserve. Do not reopen architecture design and do not begin Stage 27C. First reproduce/copy the Stage 27B proposal changes from that branch onto your isolated branch without creating intermediate commits, then correct the known missed PR #529 review findings that now affect the normative Stage 27 contracts and the Stage 27B runtime implementation. Scope is Stage 27 docs/contracts/workbench/tests only: no V3/ratings, production access/writes, deployment, Journal ingestion/Commander History product activation, CRE/CPE mechanics changes, System Map product implementation, infrastructure, or production R3F cutover/removal. Address all still-applicable #529 findings: (P1) SpatialContribution ownership must support the useful base ED-Finder scene and explicit Colonisation contribution without mislabeling as Finder/CRE/CPE; choose clear renderer-neutral owner values consistent with the product contract. (P1) picking/runtime events must return the complete bounded overlap candidate set to React, with explicit truncation state, not a single arbitrary target; update PickingService/runtime/tests/workbench accordingly. (P2) SystemSceneContract must carry selection continuity independently of camera focus. (P2) normative runtime telemetry/events must represent required CPU/GPU frame durations where available, streaming latency/truncation, pick latency, recovery outcome, visible count, draw calls, resources and buffer bytes rather than an ambiguous frameMs-only shape; keep unavailable hardware metrics nullable/honest. (P2) ring truth must be a discriminated union so UNKNOWN/ABSENT cannot carry contradictory bands and PRESENT has an explicit valid band payload policy. (P2) architecture docs must preserve current brownfield plan persistence ownership in ED-Finder/Colony Planner until an explicit future migration to CPE, rather than claiming nonexistent current CPE persistence. (P2) system-map data-readiness docs must mark atmosphere/composition/etc fields not actually exposed by the current system-detail API as not exposed, and re-check the nearby stellar/orbital exposure claims against current API models/query. (P2) stage-27a production coverage SQL must count distinct physical ring bands rather than duplicate source rows, with an explicit unnamed-band policy. (P2) remove/replace any inaccurate claim that the read-only audit takes 'no locks'; state no explicit locking clauses and acknowledge PostgreSQL ACCESS SHARE locks plus timeout/concurrency controls. Also verify the original Stage 27B repair goals remain satisfied: canonical representation classes/BodyRef/SystemScene/RuntimeCommand/RuntimeEvent boundary, public no-Babylon contract, resource-loss recovery hooks for WebGL context loss/restoration and WebGPU device loss where supported, deterministic recovery tests, isolated Babylon 9 workbench, production R3F untouched. Run focused frontend tests/typecheck/build/workbench E2E where available, docs/static checks and git diff --check. Do not fake GPU/WebGPU hardware evidence; report limitations honestly. End with changed files, tests/evidence, and residual risks. Do not merge or deploy.",
  "mode": "implement"
}
