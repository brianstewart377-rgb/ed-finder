{
  "mode": "implement",
  "task": "EMERGENCY V3 RUNTIME LANE — Hetzner V2 expires in under 48 hours. Work from current origin/main and issue #519. Do not deploy, access production, use production credentials, alter DNS, or mutate any live database. Implement the smallest production-quality V3 runtime that can remain usable after Hetzner disappears. The retained PG18 database is external and must be treated as existing state, not rebuilt/restored from V2. Requirements: (1) create a SEPARATE V3 production compose/config path rather than relabelling the rehearsal/V2 compose; (2) boot the API against externally supplied PostgreSQL role URLs without ever starting or depending on bundled PostgreSQL 16; (3) boot Redis with an explicit persistence policy; (4) boot NATS if it is part of the actual application topology; (5) replace/avoid any placeholder sleeping worker and start only real worker/maintenance/EDDN processes that exist in the repository, documenting any genuinely missing worker implementation; (6) give API, Redis, NATS, workers and proxy appropriate restart policies, health checks, bounded resources and production-safe logging; (7) private validation first: bind the proposed proxy/runtime so it is not publicly exposed by default, with an explicit later operator-controlled public/cutover step; (8) do NOT replay the V2 migration manifest against the retained V3 candidate and do NOT invent a V3 identity schema. Frontier OAuth must remain safely disabled/gated if the V3 identity contract is unavailable; the API should still be able to boot for non-authenticated read journeys if its real dependencies permit it; (9) preserve all current V2/default deployment behaviour; (10) add secret-safe environment validation, deterministic start/status/stop/smoke commands, tests proving the V3 runtime cannot start bundled PG16, and an operator runbook with exact private-health validation commands. Inspect current compose/services/code and map service names to reality rather than assuming names. You may use issue #519 and existing remote PG18 hardening branches as read-only design evidence, but keep this branch focused on runtime. Run all focused tests/lints/compose validation available without credentials. End with exact files changed, exact services that will boot, exact remaining blockers that require retained Phase 4C artifacts or owner-only secrets, and the implementation branch/commit."
}