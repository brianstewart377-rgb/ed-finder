{
  "mode": "implement",
  "task": "Remove Playwright from ED-Finder completely and migrate any genuinely unique browser coverage to the existing Cypress 15.21.1 release-gate/Review Lab path. Read CLAUDE.md and relevant docs first. Work from current main. Requirements: (1) remove @playwright/test and @axe-core/playwright plus all Playwright-only package scripts/config/spec/helper/runtime files that are no longer needed; regenerate frontend/yarn.lock correctly with Yarn v1; (2) remove Playwright browser cache/install/runtime from .github/workflows/ci.yml and stop the two expensive Playwright shard stacks; branch protection currently requires the check context exactly named 'Frontend v2 E2E (Playwright)', so preserve that job/check NAME temporarily but make the job run/validate Cypress with zero Playwright runtime, ideally as one release-gate job rather than two shards; do not weaken hard failure behavior; (3) migrate Review Lab browser execution from scripts/dev/review_lab/browser_runner.py and frontend/e2e/review-environment.spec.js to Cypress while preserving isolated-loopback/no-production containment, browser summary/evidence artifacts, accessibility/keyboard checks that are genuinely unique, and Review Lab's required check name; remove Playwright Chromium install from .github/workflows/review-lab.yml; (4) update tests/test_e2e_harness_contract.py and any related contracts to fail closed if Playwright packages/config/runtime commands remain and to assert Cypress retries stay 0; (5) clean up stale Playwright reports/artifact naming/docs/comments where appropriate, but do not broaden into product behavior; (6) preserve production behavior exactly; no deploy, no production access, no credentials. Spread independent inspection/porting across parallel agents where appropriate. Run focused Python harness tests, frontend typecheck/lint/build, Cypress release-gate tests if feasible, and Review Lab focused verification if feasible. End with concise files changed/tests/risks. Do not commit or push; wrapper handles that."
}