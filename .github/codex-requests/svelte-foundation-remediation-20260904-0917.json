{
  "mode": "implement",
  "target_branch": "frontend/svelte-foundation",
  "task": "Remediate the first Svelte foundation candidate on frontend/svelte-foundation. Start from and require exact current target head a4742f0c8915e0c811437897e0f33e023474d174; fail closed if the branch moved before your run. Keep this a bounded foundation remediation only. Do NOT deploy, touch production/server/database state, change backend runtime, implement Babylon, remove the React reference app, port product features, or broaden scope. Fix exactly these foundation issues and any directly-caused test/config adjustments: (1) Root .gitignore contains an unanchored lib/ rule with carve-outs for frontend/src/lib but none for apps/web/src/lib. Add explicit !apps/web/src/lib/ and !apps/web/src/lib/** exceptions so future Svelte lib modules cannot silently disappear. Extend tests/test_svelte_web_foundation_contract.py to pin this. (2) apps/web/src/lib/api/client.ts currently uses a handwritten fetch/getJson implementation even though Hey API generated getHealth/getAuthSession SDK functions exist. Make the bootstrap API path actually use the generated Hey SDK/client. Keep a small stable application-facing wrapper if useful, but it must call generated SDK functions rather than raw fetch. Configure same-origin credentials, AbortSignal and throwing/normalised failure semantics cleanly. Do not make normal builds depend on a live API; keep deterministic checked-in bootstrap OpenAPI generation and generated-code drift check. Update unit tests as needed and add a contract assertion that the app-facing bootstrap client imports/calls the generated SDK rather than containing a raw fetch/getJson implementation. (3) The locked stack requires ESLint 10 with official Svelte support and Prettier 3, but apps/web/package.json currently has no ESLint/Prettier packages and its lint script merely reruns svelte-check. Add a real minimal Svelte-5-compatible ESLint flat config using ESLint 10 + official eslint-plugin-svelte (and typescript-eslint support where needed), plus Prettier 3 + prettier-plugin-svelte. Pin compatible versions in package.json/pnpm-lock. Provide separate useful scripts such as lint (eslint), format:check (prettier --check) and optionally format. Keep check as svelte-check. Update the Svelte CI job to run check, lint, format:check, tests and build. Format only the new apps/web/config files affected by this foundation; do not mass-format the legacy React tree or unrelated repo. Extend the Python foundation contract to pin ESLint 10, official Svelte plugin and Prettier 3/tooling presence. Run pnpm install with frozen final lock, generate:api + generated diff check, pnpm check, pnpm lint, pnpm format:check, pnpm test, pnpm build, python -m pytest -q tests/test_svelte_web_foundation_contract.py plus directly affected CI contract tests, and git diff --check. Preserve existing React/Yarn protected jobs and Cypress jobs. Commit all remediation changes to frontend/svelte-foundation and report exact commit SHA, files changed and validation."
}