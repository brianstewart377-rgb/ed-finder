{
  "mode": "implement",
  "task": "Finish the Review Lab browser migration from Playwright to Cypress as a bounded CI/test-infrastructure change. A WIP implementation already exists on remote branch ci/review-lab-cypress. Start from current main as the wrapper requires, fetch origin/ci/review-lab-cypress, inspect its diff carefully, and incorporate the useful WIP changes onto your isolated implementation branch (do not blindly trust them). Preserve the existing Review Lab safety/containment, scenario, viewport, accessibility, browser-summary JSON schema, network-policy and product-observation contracts. Review Lab must execute pinned Cypress 15.21.1 rather than Playwright and its workflow must stop installing Playwright Chromium. Fix known WIP issues in frontend/cypress/e2e/review-environment.cy.js: assertText must not call contain.text with undefined for RegExp patterns, and system-detail close assertions must accept either hidden or absent DOM state as the legacy Playwright toBeHidden contract did. Update tests/test_local_review_test_environment.py and any other focused structural tests so they assert the new Cypress command/config/artifact contract rather than the old Playwright runner; do not weaken or skip those tests. Keep ordinary Cypress parity limited to release-gate.cy.js and keep Review Lab on a separate Cypress config/spec. Do NOT delete the legacy Playwright package dependencies/config/spec files yet in this task; this task is only to prove the replacement Review Lab path green before the final deletion slice. Do not change product behavior, deploy, access production, or use production credentials. Run focused Review Lab/unit/contract tests plus appropriate JS syntax/lint checks and git diff --check. End with files changed, exact tests run/results, and remaining risks." 
}
