{
  "mode": "implement",
  "target_branch": "migration/svelte-cypress-hard-cut",
  "task": "Repair the remaining exact-head Review Lab telemetry keyboard failure on PR #578 branch migration/svelte-cypress-hard-cut, expected at 5653c80c2103230f6cf18e34e90fcde9ce037743. Use first-hand runtime evidence from Review Lab run 33951393579: all alpha/beta/gamma/delta scenarios passed, the expected Delta 503 then provenance 200 sequence was captured, API/console/static/teardown passed, and profile failure attribution worked; the only fatal error was Cypress `.type('{enter}')` declaring the already-focused native `planner-telemetry-dock-toggle` not visible after the helper had reset scroll and proved its interior hit target. Read CLAUDE.md, frontend/cypress/e2e/review-environment.cy.js, tests/test_local_review_test_environment.py, the failure artifact/browser-summary from run 33951393579 if accessible, and the retained Playwright keyboard helper only as behavioural evidence. Implement a narrow collector fix, not a product CSS/markup workaround. Eliminate the actionability-sensitive stale `cy.focused().should(...).type('{enter}')` sequence. Re-query the unique current whole-system planner, telemetry region and native button immediately before each activation; focus the actual DOM button with `{preventScroll:true}` where supported; positively prove `document.activeElement` is that exact current button; activate through a real keyboard Enter event/default-button-action path, reusing or adapting the existing `emulateFocusedButtonEnterDefaultAction` helper rather than calling React handlers, mutating aria/data attributes, or force-clicking. Re-query after each state transition and retain the full false->true->false assertions for button aria-expanded, region data-mobile-dock, controlled panel id/linkage and data-open state. Preserve the evidence-disclosure fixes, scenario ordering, 5-profile matrix, overflow checks, failure attribution and browser-summary schema. Add focused Python contract tests that reject `.type('{enter}')` and stale chained subjects in telemetry, require preventScroll focus/current-element identity/native keydown+keyup/default-action handling, and require re-query/state proof after both transitions. Run the focused Python tests and full Review Lab verification if practical; inspect Cypress failure artifacts on any failure and fix the next exact cause rather than weakening actionability or using `{force:true}`. Do not change product source, deploy, access production/database/secrets/OAuth/Stage27, or merge PRs. Leave the branch ready for fresh exact-head checks."
}
