{
  "mode": "investigate",
  "task": "Establish the shortest existing access path for the non-production V3 checkpoint on Contabo. PR #622 is merged as f9d3842445025e57985a9585c02d6e30bc190561, but the GitHub-hosted provisioning run 34057771181 stopped before SSH because V3_LIVE_CHECKPOINT connection secrets were absent. The owner wants us to reuse existing connected access rather than ask them to wire a second SSH path. This is READ-ONLY investigation, not deployment or privilege changes. Read CLAUDE.md and the current checkpoint workflows/runbooks. On the worker's own machine, report sanitized hostname/FQDN, effective user/UID/GID, machine architecture, the exact active actions.runner service names, installed CPython3.14/Docker/Compose/PostgreSQL/nginx presence, and whether this local runner can execute the existing reviewed provisioner with its required non-root operator identity via already-existing noninteractive sudo. Use id/hostname and sudo -n -l or equivalently read-only permission probes; do not change sudoers, users, groups, services, packages, files outside the wrapper's normal test environment, credentials, database, containers, or network. Never run the provisioner, never access production, never print any key, token, credential-bearing environment or secret file contents. If sudo requires authentication, report that as a concrete capability limit. Inspect existing repository-controlled execution paths for an already-authorized way to run the reviewed non-production checkpoint operation locally on these existing Contabo runners. Give exact file paths and the minimal transport-only changes needed, if any, for BOTH provision and canonical web/API deployment without new SSH secrets, retaining trusted-main-only code, environment restriction, host/three-runner checks, deployment lock, immutable release authentication, read-only preview data, receipts and no production access. End with a compact sanitized capability receipt in stdout and an actionable recommendation. Do not modify the repository or commit/push anything."
}
