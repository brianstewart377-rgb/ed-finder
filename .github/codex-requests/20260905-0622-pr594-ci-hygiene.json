{
  "task": "Repair two exact CI/security findings for draft PR #594 on isolated child branch migration/svelte-shared-core-ci-hygiene, created from PR head 1b8f23976f9d1f06feb46e3b90d619d0ac21a883. Read CLAUDE.md, packages/api-client/src/core.ts and core.test.ts, frontend/knip.json, frontend/src/types/{api,api.gen}.ts, frontend/scripts/types-gen.mjs, packages/api-client/src/{types,index}.ts and generated/api.gen.ts, and the relevant CI source-presence/OpenAPI drift steps. First, address GitHub Advanced Security comment discussion_r3939700665: configureApiBase currently uses `base?.replace(/\/+$/, '')`, which CodeQL flags as a polynomial regular expression on uncontrolled input. Replace it with bounded linear-time trailing-slash removal without changing public behaviour for undefined, normal bases, repeated trailing slashes, roots, or absolute URLs. Add focused tests including a very long repeated-slash suffix and all-slash/empty edge cases; do not add another vulnerable regex or impose an arbitrary truncation. Second, fix `yarn knip --files` reporting `frontend/src/types/api.gen.ts` unused. That file is an intentional compatibility re-export while authoritative generation writes `packages/api-client/src/generated/api.gen.ts`, and CI still requires the frontend path. Keep the compatibility shim and generated authority; register it honestly as a public/compatibility entry in Knip rather than deleting it or blanket-disabling unused-file checks. Add/update a focused repository contract test if one exists so the compatibility entry cannot silently disappear. Run package shared check/lint/tests/build, React typecheck and `yarn knip --files`, relevant OpenAPI drift/source-presence Python contracts, apps/web checks if affected, CodeQL-friendly static inspection, and git diff --check. Do not touch colony-project snapshot equality (a separate worker owns it), Review Lab, WP2 persistence work, feature UI, routing, production, deployment, DB, OAuth, secrets, or Stage 27. Leave this child branch ready to combine into migration/svelte-shared-core; do not merge branches.",
  "mode": "implement",
  "target_branch": "migration/svelte-shared-core-ci-hygiene"
}
