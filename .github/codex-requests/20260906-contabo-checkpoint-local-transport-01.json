{
  "mode": "implement",
  "task": "Implement the smallest coherent transport correction for the Contabo V3 checkpoint, on a new isolated branch from current main. PR #622 is merged; no application or Svelte migration work remains in this task. Its first provision request (run34057771181) stopped before host access because a new GitHub-hosted-to-Contabo SSH credential path was not configured. The owner explicitly asks to get the app live using existing connected access without making them configure five new SSH secrets. Replace that unnecessary SSH transport for BOTH provisioning and canonical application deployment with a bounded trusted-main workflow job running locally on the existing three Contabo Codex runners. This task only changes/tests repository code; DO NOT provision/deploy now, change users/sudoers/runner services/packages/host credentials/database, access production or print secrets. Inspect actual runner hostname/user/UID/GID and sudo -n -l READ-ONLY to verify feasibility; if no current noninteractive privilege permits the reviewed provisioner, stop and report the actual limitation rather than adding privilege or inventing credentials. Reuse existing runner labels, verify exact hostname/FQDN/architecture/account and exactly-three active runner set before any privileged action. Do not start Codex inside operational provision/deploy jobs. Keep issue623 owner-only strict comment control, use only reviewed immutable trusted-main workflow/source, prohibit branch-supplied operation code, preserve environment v3-live-checkpoint, preserve canonical deployment concurrency and durable host lock, readonly preview database, exact Python3.14, image manifest/provenance/digest/schema checks, bounded resource limits, deploy/rollback/smoke logic and sanitized receipts. Keep release build on GitHub-hosted CI. Preserve the source-free disposable bundle boundary instead of deploying from a mutable coding worktree, and preserve no-push-credential separation. Address already-running runner processes not acquiring newly-added docker group membership without restarting any of the three runner services (a narrowly used existing sudo/runuser capability can refresh group membership while running the canonical deployer as the non-root operator). GHCR remains per-deploy ephemeral packages-read only, never a permanent token; login/logout and failures must be safely scoped. Add a real public-FQDN smoke check in addition to the canonical loopback smoke so success means the owner's browser can reach the checkpoint, not just a local port. Limit changes to the existing two checkpoint workflows, narrowly necessary local transport helper/tests, and current checkpoint docs. No new framework, operator product, credential provisioning system, redesign, or general remote shell. Update obsolete SSH-specific contract tests to assert the equally guarded local transport, retain underlying provisioning regression coverage and add executable local-wrapper safety tests with all services/sudo stubbed. Run state resolver and focused CPython3.14 tests/lint/shell syntax. End with exact changed paths, test evidence, capabilities observed (sanitized), and how to trigger the existing issue623 provision/release/deploy sequence after review/merge. Do not trigger paid Octopus reviews or deploy from your implementation branch."
}
