{"mode":"implement","target_branch":"cleanup/v3-authority-and-legacy-boundaries","task":"Implement the remaining low-risk V3 authority/legacy-boundary cleanup identified by the post-#568 forensic audit on this existing branch. Read CLAUDE.md, README.md, docs/operations/infrastructure-status.md, operator-command-contexts.md and all affected tests first. Requirements: (1) remove the retired provider from live runtime strings: change apps/api/src/config.py default app_version from 3.0.1-hetzner to an appropriate host-neutral V3 value, make apps/api/src/main.py docstring/startup log host-neutral, and remove/update the stale Hetzner hardware header in apps/importer/src/import_spansh.py; update snapshots/contracts; (2) add prominent executable-file banners to scripts/restore_postgres_backup.sh and scripts/rehearse_postgres_restore.sh stating local-dev/CI rehearsal only, never V3 production, and that V3 database recovery remains fail-closed under infrastructure-status.md; preserve their local behaviour and add matching tests; (3) add a clear top-level header to docker-compose.yml declaring it the retained legacy/self-host/local application stack, not V3 production; explain PostgreSQL 16 versus production PostgreSQL 18 and state apps/maintenance is not the V3 backup/PITR design; do not remove services relied upon by CI/importer/monitoring; add contract coverage; (4) label apps/maintenance documentation/comments consistently as legacy/self-host/local, not V3 backup authority; (5) add a superseded/non-authoritative banner to docs/operations/migration-ledger-implementation-plan.md where it names deleted release paths; annotate docs/development/chatgpt-ops-control-plane.md so proposed mutating operations are explicitly design-only/not implemented in the current allowlist; fix stale links in audit-remediation-plan.md and database-bloat-incident-2026-08-20.md where appropriate; preserve dated historical audits as history; (6) add a concrete legacy-data inventory document and machine-readable manifest/template that classifies public/reconstructable data versus irreplaceable/private/manual/history data, records the retained dump identity, requires table/record counts and validation before selective extraction, and explicitly does not claim migration completeness without evidence; add a safe read-only inventory helper only if it can operate against an explicitly confirmed disposable/offline restored dump and never production; no restore/extraction or DB access in this task; (7) search the current tree for stale live authority wording and remove ambiguity without deleting deliberate tombstones or Stage evidence; (8) run focused tests, backend unit contracts, Ruff and git diff --check. Do not deploy, access production, use secrets, restore a database, mutate any host or create another PR. Commit and push all coherent changes to the existing target branch."}