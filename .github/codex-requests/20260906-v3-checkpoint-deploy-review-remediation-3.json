{
  "task": "Remediate the five substantive Codex Review findings on PR #619 exact head bb0230ffbec3af8a6c162ae97bd87b108da18c98 in one coherent repository-only pass on target branch checkpoint/v3-first-live-deploy. DO NOT deploy anything, access or mutate Contabo, production, databases, DNS, secrets, registry credentials, or host services. Preserve the immutable-release, isolated non-production checkpoint, read-only database, fail-closed target-authority, digest-pinned, no-build/no-git-pull-on-host design. Keep Svelte/product/Search/Grid/Cluster work and production cutover out of scope. Resolve and regression-test these exact findings: (1) P1 scripts/operator/v3_checkpoint_deploy.py: eliminate the TOCTOU gap between verify_database_schema() and docker compose. After verifying the authorised api_env_file and its DATABASE_URL, freeze an immutable verified snapshot (or equivalently lock/revalidate the exact bytes) and ensure both candidate compose and rollback use only that verified snapshot, never rereading a mutable external env file after the schema check. Fail closed on replacement/edit/symlink/path drift and clean up safely. (2) P2 scripts/operator/v3_checkpoint_deploy.py: track env_file_consumed_by_compose independently and set it true once a compose up/recreate command that consumes the verified env snapshot completes successfully, so subsequent readiness/container/smoke/receipt failures persist accurate audit metadata; preserve false when Compose never completed. (3) P2 scripts/operator/v3_checkpoint_deploy.py: treat dangling current.json symlinks or other lexical prior-state entries as existing invalid prior state during preflight, before any image pull or service mutation. Do not let Path.exists() convert a dangling durable pointer into bootstrap absence. Add tests proving fail-closed zero-mutation behaviour. (4) P2 .github/workflows/v3-application-live-checkpoint-preflight.yml: authenticate/validate the selected release workflow run provenance before parsing or executing any artifact checksum list. Do not run GNU sha256sum --check against untrusted checksum-file paths. Either reorder authoritative run metadata validation before artifact checksum processing and then use a basename-constrained verifier, or otherwise make checksum verification incapable of referencing arbitrary runner paths. Add workflow contract tests covering malicious absolute/parent-path checksum entries and untrusted run metadata. (5) P2 scripts/operator/v3_checkpoint_deploy.py: parse docker network inspect and enforce the authorised private topology before pulls/mutation: expected local bridge network/scope and bootstrap/upgrade attached-container allowlist only. Reject unexpected peers, aliases, wrong driver/scope, malformed/unverifiable output, or topology drift that could let another container reach/spoof the API. Add focused bootstrap and upgrade tests, including an unexpected api alias peer rejection. Also verify the three older review findings from the previous pass remain fixed (rollback has no registry dependency, reviewed compatible migration identities flow through release sealing, Docker context is bound to unix:///var/run/docker.sock) without broad refactors. Run focused checkpoint/release/workflow contract tests, relevant lint/type checks, and git diff --check. Deliver one sealed implementation commit and push it through the governed trusted-push path to the existing target branch. Do not merge PR #619 and do not provision or deploy any host prerequisites in this worker.",
  "mode": "implement",
  "target_branch": "checkpoint/v3-first-live-deploy"
}
