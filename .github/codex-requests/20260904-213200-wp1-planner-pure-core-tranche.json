{
  "task": "Create an isolated implementation branch from the exact current origin/main and implement the first dependency-closed WP1 tranche of the React-to-Svelte hard cut. This task is pure-domain code only; do not touch PR #578 or #585 branches directly. Study CLAUDE.md, docs/ROADMAP.md, docs/development/v3-application-stack-decision.md, issue #580, and the current source/tests for: frontend/src/features/system-detail/simulation-preview/utils/placementHelpers.ts; frontend/src/features/system-detail/simulation-preview/optimiser/comparison/comparisonEngine.ts; frontend/src/features/system-detail/simulation-preview/validation/validationUtils.ts; frontend/src/features/system-detail/simulation-preview/exportArtifacts.ts; and their direct framework-neutral type/helper dependencies. Port a coherent, dependency-closed copy into apps/web/src/lib/domain/planner/ using Svelte-target-owned modules and Vitest tests. Preserve behaviour and assertion intent exactly, including deterministic resequencing, build-source comparison semantics, complete validation request/fingerprint identity, and export separation of planned/projected/observed/inferred/warehouse evidence. Do not create a repository-root JavaScript monorepo or packages/* workspace. Do not import runtime code from frontend/ into apps/web. Do not change React source or tests, application UI, routing, persistence, API calls, generated clients, workflows, backend, dependencies, or lockfiles unless a truly missing type-only dependency is unavoidable; prefer local framework-neutral target types. Treat id64 values as decimal strings where these target types carry identifiers; never introduce JavaScript number conversion. Port only the tests belonging to the chosen dependency-closed tranche and add a short target README/index only if needed to explain authority; no broad docs rewrite. Run the original focused React pure tests, the new apps/web Vitest tests, pnpm check, lint, format:check, and build. End with a clear mapping of old module/test to new module/test and any exact behaviour deferred because it was not dependency-closed. Let the wrapper create and push the isolated codex/run-* branch. No production, database, deployment, recovery, secrets, OAuth, DNS, or live-data action.",
  "mode": "implement"
}
