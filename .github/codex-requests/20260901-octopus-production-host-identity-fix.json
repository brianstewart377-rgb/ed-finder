{"task":"Implement a narrowly scoped production host-identity correction for the Octopus operator actions. A read-only production-status run on the actual Hetzner target proved the server short hostname is exactly `ed-finder` (not `ed-finder-prod`), PWD is `/opt/ed-finder`, and no DB/migration changes were made. Update `scripts/operator/actions/octopus-qdrant-healthcheck-repair.sh` so its fail-closed short-host guard expects exactly `ed-finder`; do NOT remove or broaden the host guard. Also inspect `scripts/operator/actions/octopus-edge-status.sh`: it currently expects short host `ed-finder-prod` and FQDN `nb79a3d.mevnode.com`; correct only the stale short-host expectation to `ed-finder` while preserving the exact FQDN check unless repository evidence requires otherwise. Add/adjust focused tests so the correct host is accepted and any other short host is rejected. Preserve every existing repair safety property: exact images/version, exact broken-or-fixed healthcheck state, backup/rollback ordering, compose validation before rollback commit, `--no-deps` service scope, read-only Postgres health verification, no DB/volume/migration/env mutation, machine-readable receipt, and fail-closed behavior. Do not touch `config/nginx.conf`, production-local files, unrelated workflows/services, databases, or volumes. Do not access or deploy to production. Keep the diff narrowly scoped to the two Octopus operator scripts/tests/docs only if necessary.","mode":"implement"}