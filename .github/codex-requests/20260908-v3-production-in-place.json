{"mode":"implement","task":"Implement the missing reviewed V3 production deployment authority for the existing single-host production environment, from current main. Do NOT mutate production and do NOT deploy anything during implementation. Production is ed-finder-prod / nb79a3d.mevnode.com. Fresh read-only host-status evidence from run 34202433965 shows production currently runs edfinder-v3-public-auth-edge, stale edfinder-v3-api:phase4c-r5, edfinder-v3-proxy, edfinder-v3-support-redis, edfinder-v3-support-nats, retained PostgreSQL container edfinder-v3-phase4c-full-20260827_r5-postgres, Octopus services, and separate pg lab/bench containers. Public UI is still the temporary replacement shell. Goal: create a fail-closed production-in-place release/deployment path that promotes the current immutable V3 backend and Svelte web onto ed-finder-prod while preserving the existing PostgreSQL 18 database/data, TLS/public-auth edge, Octopus, and unrelated containers. Requirements: create an explicitly V3 production Docker Compose/deployment authority; legacy/root Compose must not become production authority. Reuse/adapt immutable OCI backend+web release manifest/digest/build_sha/schema-identity conventions where sensible but keep production authority separate from the Contabo checkpoint. Deployment must update only necessary app services; never recreate/drop/replace PostgreSQL or its volumes; preserve current edge until verified cutover; no builds, dependency resolution, or git pull on production. First promotion must inspect existing production migration ledger/schema identity and proceed only when compatible; do not run migrations or application-data writes unless an already-current explicit production migration authority exists, otherwise emit a concrete fail-closed blocker. Do not remove Redis/NATS or migrate to Valkey in this PR. Do not install host Python 3.14 merely to make old status tooling happy; status/preflight may use bounded shell/container inspection or another already-present runtime. Add read-only production preflight/inventory that can report actual containers/listeners/current release/schema compatibility without writes. Add rollback to the prior accepted immutable application release only when schema-compatible. Add machine-readable receipts, exact host/FQDN guards, secret-safe handling, protected tests/contracts, docs, and explicit no-production-execution boundary. Contabo vmi3542235 is available only as an optional non-production validation/rehearsal box; the owner authorizes installing any prerequisites needed there (Python 3.14, Docker/Compose, nginx, test services, etc.) if useful, but Contabo must not become production authority and no production data/credentials may be copied there. Inspect existing checkpoint/release/deploy code for reusable safe components rather than duplicating blindly. Keep scope to production application promotion; no unrelated architecture cleanup. Run relevant Ruff/pytest/Semgrep or repository-standard validation, inspect diff, commit and push only through the governed Codex lane. Do not merge. Do not trigger production operations. Report exact result branch/SHA, changed files, validation, and any concrete blockers."}
