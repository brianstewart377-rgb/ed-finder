{
  "mode": "implement",
  "task": "Fix the concrete release packaging failure observed in canonical release run34059185729, web build job101556547076, on main f9d3842445025e57985a9585c02d6e30bc190561. Backend image built successfully. Web failed at apps/web/Dockerfile line18 RUN pnpm install --frozen-lockfile with ENOENT scandir '/workspace/packages/api-client'. The Dockerfile copies only apps/web files, while apps/web/package.json has file:../../packages/api-client and shared:build compiles ../../packages/api-client and ../../packages/planner-core. Implement the minimum complete shared-package build-context correction, preserving pinned Node24/nginx digests, pnpm11.25.0/frozen lock, build-SHA attestation, CYPRESS_INSTALL_BINARY=0, static output and API/frontend route ownership. Inspect real package exports, local dependency graph and build configs; ensure all required shared files are present at install/build time and any shared build ordering needed by the actual source is correct. Do not change app behavior or perform further Svelte migration. Add focused regression tests in a NEW separate test module, and add an actual apps/web/Dockerfile build validation to the appropriate existing GitHub-hosted protected container-parity/CI lane so green CI can no longer omit the deployable Svelte image. This check must not publish images or need live-host/production credentials. Another worker is correcting checkpoint transport and owns the two checkpoint workflows plus provisioning/transport tests and docs; do not modify those overlapping files. You may change apps/web/Dockerfile, narrowly necessary .dockerignore handling if actual context demands it, an existing GitHub-hosted image validation workflow, and your new targeted test file. Do not install Docker or alter services on Contabo to test it. Run state resolver, focused source/contract tests, and available frozen frontend/shared build checks; report honestly whether a real container build could run. The subsequent PR CI/canonical release will supply actual container-build evidence. Work on the new isolated branch provided by the wrapper; no deploy, production access, secrets, commits or pushes by Codex itself. End with exact changed paths, test results, and any real remaining build prerequisite."
}
