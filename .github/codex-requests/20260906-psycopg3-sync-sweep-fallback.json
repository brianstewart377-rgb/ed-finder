{
  "task": "Fallback implementation worker from the exact #601 head. Scope ONLY active synchronous PostgreSQL dependency/source migration to Psycopg 3; do not broadly edit Python-version configuration except where required to test under the repository's 3.14 V3 authority. Keep apps/api async PostgreSQL ownership on asyncpg and keep EDDN async storage on asyncpg. Remove active psycopg2-binary/psycopg2/py3-psycopg2/types-psycopg2 authority and port synchronous/importer DB code semantically to `psycopg` / `psycopg.sql` / Psycopg 3 row factories and exceptions. Cover importer code, shared_contracts, maintenance/admin scripts, checks, repair/reconcile/release/backup/acceptance tooling and their tests/doubles. Correctly replace RealDictCursor, execute_batch/execute_values and transaction/autocommit behavior; preserve SQL-composition safety, bulk-write safeguards, rollback and row-count contracts. Prefer `psycopg[binary]==3.3.4` consistently unless a repository constraint requires a justified compatible pin. Remove the temporary executable psycopg2 quarantine boundary if this scoped port makes it obsolete. Run focused tests under actual CPython 3.14 and add regression guards rejecting active psycopg2 dependencies/imports while explicitly asserting apps/api remains asyncpg. End with an exact residual psycopg2-string classification. Do not deploy, access production or change schema/data. Do not commit or push; wrapper handles that.",
  "mode": "implement",
  "target_branch": "codex/psycopg3-sync-sweep"
}
