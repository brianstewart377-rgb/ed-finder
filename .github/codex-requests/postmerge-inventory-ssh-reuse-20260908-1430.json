{
  "task": "Create a new isolated implementation branch from exact current main and fix the post-merge V3 production read-only inventory workflow failure observed in Actions run 34232054299. The guard passed, but the inventory job failed before SSH because environment v3-production-readonly provided empty V3_PRODUCTION_SSH_KEY, V3_PRODUCTION_HOST, V3_PRODUCTION_PORT, and V3_PRODUCTION_SSH_KNOWN_HOSTS. Reuse the repository's already-proven read-only production operator trust boundary for inventory only: environment ed-new-operator and the existing ED_NEW_OPERATOR_SSH_KEY, ED_NEW_OPERATOR_HOST, ED_NEW_OPERATOR_PORT, ED_NEW_OPERATOR_USER, and pinned known-host secret ED_NEW_OPERATOR_SSH_KNOWN_HOSTS with ED_NEW_OPERATOR_KNOWN_HOSTS compatibility fallback if that existing contract requires it. Keep the inventory target hard-pinned to nb79a3d.mevnode.com and keep strict host-key checking. Do NOT change the preflight/promote jobs' environment or V3_PRODUCTION_* credential boundary. Do NOT activate deployment authority, deploy, install host software, mutate production, touch PostgreSQL, or weaken target/SSH checks. Add/update focused tests proving inventory uses only the existing read-only operator environment/secret names while preflight/promotion retain the separate v3-production/V3_PRODUCTION_* boundary. Update operator/runbook wording only if needed to make the split explicit. Run the targeted production deployment/workflow tests and relevant contract checks, plus formatter/linter if applicable. Commit the coherent fix on a new isolated codex/run-* branch for PR review.",
  "mode": "implement"
}
