{
  "task": "Remediate the three remaining substantive Codex Review findings on PR #619 exact head e9efab172acba4c9b2a7c99ba8aed5e7c2f4986e in one coherent repository-only pass on target branch checkpoint/v3-first-live-deploy. DO NOT deploy anything, access or mutate Contabo, production, databases, DNS, secrets, registry credentials, or host services. Preserve the existing immutable-release, non-production, fail-closed checkpoint design and keep Svelte/product/Search/Grid/Cluster work out of scope. Findings to resolve and verify: (1) P1 scripts/operator/v3_checkpoint_deploy.py upgrade rollback currently reuses command_plan() and re-pulls the previously accepted backend/web digests. The prior digests are already pulled and verified before candidate mutation, so rollback after a readiness/smoke failure must restore them without any registry dependency. Build an upgrade rollback plan containing only the necessary docker compose up/recreate operation with --pull never against the verified prior images; bootstrap absence rollback semantics stay unchanged. Add focused regression tests proving rollback does not invoke docker pull and still restores the prior digest-pinned services. (2) P1 .github/workflows/v3-application-release.yml does not expose/pass reviewed compatible migration-set identities to scripts/release/v3_release_manifest.py create. Add a fail-closed workflow_dispatch input/normalization path for zero or more reviewed sha256 migration identities, and pass them as repeated --compatible-migration-set arguments during both manifest-policy preflight and final manifest sealing when schema_compatibility=backward-compatible. Preserve exact mode semantics (source migration identity only), reject malformed/secret-like/unreviewed input, and update focused workflow/release contract tests. Do not weaken v3_release_manifest.py validation. (3) P1 scripts/operator/v3_checkpoint_deploy.py validates the configured Docker context name/config directory but not its endpoint. Before any docker pull/compose mutation, inspect the selected context and bind it to the authorised local rootful Docker daemon; reject ssh://, tcp://, remote contexts, alternate unix sockets, malformed output, or unverifiable context. The intended endpoint is the local Docker socket unix:///var/run/docker.sock. Add focused tests for accepted local context and rejected remote/alternate endpoints. Re-run the focused checkpoint/release tests, repository contracts, lint/type checks needed by touched files, and git diff --check. Do not revisit already resolved/outdated review findings except where tests require it. Deliver one sealed implementation commit and push it with the governed trusted-push path to the existing target branch; do not merge PR #619.",
  "mode": "implement",
  "target_branch": "checkpoint/v3-first-live-deploy"
}
