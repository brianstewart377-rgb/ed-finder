{
  "mode": "implement",
  "task": "OAuth is the priority. Start from the current origin/main exactly as provided by the wrapper. Inspect PR #508 and origin/codex/run-33272182485-1, which contain the refreshed Frontier OAuth / owner-access implementation and the fixes for the #490 review blockers. Port that OAuth feature cleanly onto current main without reverting or weakening the Cypress/CI changes that have landed since #508. Preserve the intended security model and specifically retain/fix: canonical OAuth login host before the host-only state cookie, authenticated protected integration requests, expired state cleanup, configured frontend API-base routing, Frontier token transport errors as 502, token response object validation, and safe proxy-aware OAuth rate-limit client identification. Resolve any current-main integration conflicts narrowly. Run focused backend auth/OAuth/session/owner tests, relevant frontend tests/typecheck/build, migration/static checks, and git diff --check. Do not deploy, access production, use production credentials, merge, or change unrelated product behavior. Produce a clean implementation branch from current main with concise test evidence and remaining external activation requirements (especially Frontier client ID/shared key/redirect URI)."
}
