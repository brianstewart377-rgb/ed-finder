{"mode":"implement","task":"Implement a narrowly scoped read-only ed-new Ops operation named `octopus-edge-topology-detail` on current main. Context: Octopus v1.0.122 is healthy internally on MevSpace host short hostname `ed-finder-prod`, FQDN `nb79a3d.mevnode.com`, web 127.0.0.1:43300, and the current read-only edge audit shows exactly one failure: `octopus_route_missing`. The live nginx containers are exactly `edfinder-v3-public-auth-edge` and `edfinder-v3-proxy`. This operation is needed only to identify the real live config/mount source paths before any bounded route install. Requirements: no caller parameters; verify exact host identity and working directory `/opt/ed-finder`; inspect exactly those two container names and fail closed if either is missing/unexpected; emit one concise machine-readable JSON receipt with, for each container: image, network mode and network names, published ports, bind/volume mounts with source,destination,type,rw, nginx `-T` configuration file origin paths, and sanitized `listen`, `server_name`, `proxy_pass`, and `include` directives. Also verify internal Octopus `/api/health` and `/api/version` on 127.0.0.1:43300 and require exact version `1.0.122`. Read-only only: do not read env files or Config.Env, private keys/cert private keys, databases, volumes' arbitrary file contents, secrets, or unrelated container config; do not modify files, containers, networks, services, nginx, databases, volumes, migrations, or env values. Docker inspect metadata and `docker exec <exact-nginx-container> nginx -T` are allowed. Do not use `docker exec` for any mutation. Do not dump complete unsanitized config; only emit the bounded origin-path/directive metadata above. Add the operation to `.github/workflows/chatgpt-ed-new-ops.yml` and the fixed dispatcher allowlist with the same trusted-main/SSH pattern as existing ed-new actions. Add focused tests proving exact container names, read-only command surface, no secret/env/DB access, structured output, fail-closed behavior, and exact Octopus version check. Do not access or deploy to production. Run focused tests and required repository validation, then push a worker branch for review."}
