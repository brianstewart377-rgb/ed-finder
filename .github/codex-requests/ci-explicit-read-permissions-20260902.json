{"mode":"implement","task":"On current main, modify ONLY .github/workflows/ci.yml. Add a top-level permissions block immediately after the on: trigger block and before concurrency: with exactly contents: read. Preserve the existing job-level changes permissions override (contents: read, pull-requests: read) and make no other file changes. Validate YAML and run the focused workflow-policy tests if available. Do not change product/runtime code."}
