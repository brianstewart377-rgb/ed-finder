{"mode":"implement","target_branch":"architecture/v3-application-stack-lock","task":"Remediate the three verified Codex P1 findings on draft PR #575 in one coherent governance-only commit. Work only on architecture/v3-application-stack-lock, whose current expected head is 61e643e234b2fc1f3915b1ceb74b16beb4ae2199. Read the actual current files before editing. FINDING 1 / routing: docs/development/v3-application-stack-decision.md currently says only /api/* routes to FastAPI, but the current backend also owns root /openapi.json (FastAPI OpenAPI, used by CI/type generation) and /s/{id64} (apps/api/src/share_router.py OpenGraph stop page). Amend the locked routing/request-shape/acceptance wording so retained backend-owned non-/api routes are explicitly proxied to FastAPI. At minimum preserve /api/*, /openapi.json and /s/{id64}; do not invent a broad catch-all that steals Svelte routes. Add focused regression assertions. FINDING 2 / authority chain: make the new stack decision genuinely authoritative without lying about the currently checked-in legacy implementation. Update README.md, docs/ROADMAP.md and CLAUDE.md so the authority/source order explicitly includes docs/development/v3-application-stack-decision.md after the programme/authorization roadmap and before implementation-specific engineering guidance where appropriate. Reconcile stale stack instructions carefully: current checked-in React/Yarn/Python-3.12 code and commands remain migration/reference/current-validation reality until their reviewed migration slices land, but the locked target for NEW V3 application implementation is Svelte 5/SvelteKit 2/TypeScript 6, Node 24/pnpm 11 and CPython 3.14+uv per the stack decision. Do not rewrite Stage 26 history or claim current source already uses Svelte/pnpm/Python3.14. CLAUDE should tell agents to follow the stack decision for new architecture while using current toolchain commands only when validating still-current legacy code. Preserve Stage 27A authorization boundaries. ROADMAP should register the stack lock as current application technology authority without opening Stage27B or Babylon runtime. README should link it in canonical sources and explain current frontend vs target succinctly. FINDING 3 / rollback: the stack decision currently implies selecting a previously accepted app release manifest is sufficient rollback. That is unsafe after destructive/incompatible schema changes (e.g. sql/043_drop_ratings_score_breakdown.sql) and there is currently no executable V3 DB recovery runbook. Amend release/rollback rules so an old application manifest is a valid rollback target only when backward schema compatibility with the current DB has been proven (prefer expand/contract/backward-compatible migration discipline). If compatibility is not proven, rollback must fail closed and require an explicitly reviewed/rehearsed database rollback/recovery path before the old app can be promoted. The release manifest/acceptance should record migration-set/schema compatibility/rollback eligibility, and incompatible destructive migrations must not advertise one-click app-only rollback. Do not invent an executable DB rollback procedure in this PR. Extend tests/test_v3_application_stack_decision.py to pin all three corrections, including authority-chain registration/current-vs-target distinction and explicit backend route ownership/schema-compatible rollback. Run that test plus affected Stage27/governance/decommission/README/agent-contract tests you discover, and git diff --check. Do not modify frontend source/package/lockfiles, runtime code, Docker/Compose/workflows, migrations, server state or production. Do not merge or trigger production operations. Commit and push one coherent remediation commit. Report exact SHA, files changed and test results."}