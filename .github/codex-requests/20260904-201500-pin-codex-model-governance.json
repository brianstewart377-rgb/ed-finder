{
  "task": "Implement a fail-closed Codex model-governance change for the ED-Finder coding worker. Read CLAUDE.md and the current authority documents first. Limit this task to the Codex request/worker control plane, its contract tests, and directly relevant documentation; do not change product code. The existing worker currently invokes `codex exec` without an explicit model. Change every active investigation and implementation invocation so the CLI receives the explicit official model ID `gpt-6-astra` through the documented `--model`/`-m` option and receives `model_reasoning_effort=\"xhigh\"` through documented Codex CLI configuration syntax. Hard-pin these values in trusted workflow code rather than accepting arbitrary model or effort text from request JSON. Fail closed if the installed Codex CLI does not support the required flags/configuration or if the invocation would fall back to another model. Before invoking Codex, print a sanitised attestation containing the Codex CLI version, model ID, reasoning effort, request ID, mode, selected branch, and immutable base SHA; after implementation, record the result SHA in the normal wrapper output/job summary. Add non-networked repository contract tests that parse the active workflows and prove: every `codex exec` is explicitly pinned to `gpt-6-astra`; every invocation pins xhigh reasoning effort; no request-controlled model override or insecure fallback exists; investigation and implementation paths use the same model contract. Preserve immutable action SHAs and all existing branch, state, no-production, no-secret, commit and lease-safety gates. Run focused tests plus YAML parsing. Do not deploy, access production, database, DNS, OAuth secrets, or recovery systems. Create the implementation on a new ordinary branch from current main and leave a concise result suitable for opening a PR to main.",
  "mode": "implement",
  "target_branch": ""
}
