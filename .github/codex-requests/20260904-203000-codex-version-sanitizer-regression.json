{
  "task": "On current origin/main, create a new ordinary implementation branch and add a narrowly scoped regression contract for the Codex CLI-version sanitiser repaired by PR #592. The trusted wrapper must first attest model gpt-6-astra with xhigh reasoning; if model selection or attestation fails, stop before editing. Read CLAUDE.md, .github/workflows/codex-laptop.yml, tests/test_codex_model_governance.py, and the relevant control-plane documentation. Keep the existing working sanitiser unless a demonstrably safer equally bounded expression is required. Add a non-networked test that extracts or faithfully executes the active `Verify required Codex model contract` version-sanitising pipeline against representative output `codex-cli 0.116.1-dev.1`, proves it exits successfully, preserves the expected safe text, strips newline/control or shell-hostile characters, and prevents recurrence of the invalid reversed `tr` range form from PR #591. The test must validate the active workflow rather than merely duplicate a separate hard-coded safe command. Preserve all model pinning, xhigh effort, strict/ignore-user-config flags, request immutability, state gates, sealed result, privileged workflow push and exact-head lease controls. Touch only the worker workflow if Codex finds a necessary correction, tests/test_codex_model_governance.py, and directly relevant control-plane docs; no product code. Run the focused governance tests, tests/test_codex_async_bridge.py, YAML parsing, git diff --check, and any inexpensive relevant lint. Leave a concise result suitable for a PR to main, including starting/ending SHAs and exact model/effort/run attestation. No production, database, DNS, deployment, recovery, secret or live-data action.",
  "mode": "implement"
}
