{"mode":"implement","task":"Prioritize getting Frontier OAuth PR #490 ready on current main without deploying or touching production. Start from current main as the wrapper requires. Fetch and inspect branch codex/frontier-oauth-owner-access / PR #490 and port its intended OAuth owner-access implementation onto this fresh implementation branch, resolving conflicts against current main rather than blindly copying stale files. Address all still-live Codex review findings on #490, especially: (P1) www.ed-finder.app login must not lose the host-only OAuth state cookie when the callback URI is apex ed-finder.app; canonicalize/align host behavior safely and test it, (P1) integration tests for newly protected /api/status and /api/cache/stats must authenticate correctly while preserving intended public minimal health behavior, (P1) OAuth callback rate limiting must key on the actual trusted client address behind nginx without trusting arbitrary forwarded headers, and (P2) validate successful token endpoint JSON is an object/mapping before reading fields. Also verify whether the older outdated P2 findings (expired-state cleanup, VITE_API_BASE login routing, httpx transport failure handling) are already fixed in #490; carry forward the fixes if present or implement them if not. Preserve server-side ADMIN_TOKEN automation compatibility and the existing owner/session security boundaries. Run focused Python auth/unit/integration tests, relevant frontend auth tests/typecheck, lint as appropriate, migration/schema checks needed for migration 048, and git diff --check. Do not merge, deploy, access production, or use production credentials. Keep the resulting branch focused on OAuth readiness and report any genuine external activation blocker such as Frontier application credentials/callback registration."}