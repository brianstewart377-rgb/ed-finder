{
  "mode": "implement",
  "task": "V3 production-promotion tooling, Slice 1 only: READ-ONLY PostgreSQL 18 production-candidate readiness audit + focused tests + promotion runbook. Context: we are promoting the EXISTING V3 PostgreSQL 18 rehearsal/test database to production. Do NOT restore V2 into V3 and do NOT design a fresh rebuild. Read CLAUDE.md, docs/ROADMAP.md, relevant migration tooling, scripts/checks/data_invariants.py, and existing operator/data-workflow conventions before editing. The wrapper has already run the mandatory project-state gate. Implement a strictly READ-ONLY readiness/audit command that accepts an explicit DATABASE_URL (or established repo DB convention), fails closed on invalid/unsafe configuration, never mutates the database, and redacts credentials/secrets from all human and JSON output. Produce both human-readable output and a durable JSON receipt. Report PostgreSQL version and database size. Compare the applied migration ledger/checksums against sql/migration-manifest.txt and report pending automatic migrations, pending/manual entries separately, and checksum mismatches without applying anything. Report useful base-data counts/coverage for systems, bodies, stations, and body_rings. Report full-build derived-data completion/backlogs needed for promotion, including grid_cell_id, macro_grid_id, current ratings v3.4 and rating_dirty, topology, economy_pair_synergy, archetype scores/traits and dirty state, regional analysis, station-body links/required backfills, cluster_summary/cluster_dirty, and materialized-view presence/readiness where the schema supports them. Missing optional tables/views must be reported cleanly rather than crashing the whole audit. Later/runtime feature tables (journal/evidence/exploration/powerplay/routes/frontier/accounts or equivalent present in this repo) should be reported for presence and useful row counts/status, but must NOT be falsely classified as requiring full historical population before production. Reuse scripts/checks/data_invariants.py definitions/contracts where sensible rather than inventing divergent readiness definitions. Default production-safe mode must avoid unnecessary expensive exact full-table scans across the ~188M-system dataset; if a heavy/full mode is useful, make it explicit and separate from the safe default. Add focused tests covering read-only/fail-closed behavior, migration manifest/ledger comparison, secret redaction, missing optional objects, and the distinction between full-build-required datasets and feature-populated/runtime datasets. Add or update a concise production-promotion runbook that records the safe dependency order: snapshot/backup -> schema/migrations -> base/canonical data -> grid -> ratings v3.4 -> topology/economy synergy -> archetypes -> regional analysis -> station-body links/backfills -> full clusters -> materialized views/stats/cache -> strict invariants/readiness receipt -> application deployment/cutover, with measurable zero-backlog/valid-state criteria rather than merely script exit codes. IMPORTANT SCOPE LIMITS FOR THIS SLICE: do NOT change docker-compose/deployment/external-PG18 mode, do NOT change chatgpt-ed-new-ops.yml, do NOT deploy, do NOT access production hosts or production credentials, do NOT run migrations, refresh materialized views, build/rebuild grid/ratings/topology/archetypes/regional/clusters, perform DNS/cutover, or execute any DB writes. Run focused tests/lint plus git diff --check. End with exact files changed, tests run/results, and remaining risks."
}
