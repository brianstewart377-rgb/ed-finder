{
  "mode": "implement",
  "task": "EMERGENCY PR #526 HARDENING LANE — Hetzner V2 expires in under 48 hours. Work from current origin/main and use PR #526 head origin/codex/run-33322010010-1 at commit 5d8530145893fa461801f2b34420f86cb4ca4f66 as read-only source material. Do not deploy, access production, use production credentials, alter DNS, mutate any live database, or weaken fail-closed behavior. Build a clean replacement implementation on top of current origin/main that preserves the intended PG18 production-candidate audit/external-DB deployment functionality from PR #526 while fixing EVERY current Codex review finding against that exact head. Required fixes: (1) scripts/deploy_main.sh / migration path must preserve operator-exported DATABASE_MIGRATION_URL and other already-resolved external role URLs so apply_migrations.sh cannot re-source .env and silently overwrite validated operator values; make precedence deterministic and test it. (2) scripts/validate_external_db_config.sh must never place DSNs/passwords in psql argv; use a credential-safe mechanism and test/inspect argv behavior. (3) .github/workflows/chatgpt-ed-new-ops.yml production-candidate readiness must execute the reviewed audit implementation/manifest, not an arbitrary stale /opt/ed-finder host copy; bind the retained receipt to an explicit reviewed commit/revision and fail closed on mismatch. (4) audit receipt must record credential-free stable candidate identity including current_database() and PostgreSQL system_identifier (or an equally stable documented equivalent available on PG18) so the evidence is bound to the intended database/cluster. (5) external DB validation must prove role separation/suitability, not just connectivity: capture current_user and verify each configured URL has the privileges appropriate to its declared role, including read-only reader separation and migration DDL capability, failing closed for same/overprivileged credentials where the contract requires separation. (6) topology readiness must require the COMPLETE canonical economy-pair set per eligible system, not merely any row; derive the canonical expected set from repository authority and add tests for partial-row failure. (7) topology readiness must reject stale topology relative to current ratings/source state, using a durable freshness/build-generation contract supported by current schema; if the existing schema lacks sufficient provenance, fail closed and document the blocker rather than inventing unsafe success. (8) run the entire multi-query candidate audit inside one read-only REPEATABLE READ (or stronger) transaction so the receipt describes one consistent snapshot. Preserve secret-safe JSON/human receipts, migration ledger/checksum verification, candidate non-mutation guarantees, external PG18-only mode, and existing default/V2 behavior. Add focused regression tests for all eight findings plus any existing tests needed. Run pytest, Ruff, shell syntax, git diff --check, and compose validation where available without credentials. End with exact files changed, test results, any remaining fail-closed blockers, and branch/commit. Do not merely comment on PR #526; produce the corrected implementation branch from current main so it can supersede #526 cleanly."
}
