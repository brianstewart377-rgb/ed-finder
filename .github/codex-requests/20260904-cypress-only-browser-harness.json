{"mode":"implement","target_branch":"migration/cypress-only-browser-harness","task":"Implement a complete Playwright-to-Cypress browser-harness replacement on the existing target branch. Read CLAUDE.md, docs/development/v3-application-stack-decision.md, docs/development/pull-request-acceptance-policy.md, the Playwright/Cypress audit findings in the repository discussion context, and all Review Lab code/tests before editing. The public frontend is currently unavailable, so do not preserve Playwright merely as a parallel runnable fallback. Requirements: (1) rewrite the Review Lab browser collector from frontend/e2e/review-environment.spec.js to Cypress while preserving the existing browser-summary.json schema and Python-owned validation/evaluator semantics exactly unless a schema bump is unavoidable and fully migrated; preserve all 5 viewport profiles, alpha/beta/gamma/delta evidence postures, keyboard interactions, Escape close, telemetry aria-expanded checks, Delta 503-to-provenance fallback correlation, API/console/page-error capture, product observations, and overflow metrics; (2) update scripts/dev/review_lab/browser_runner.py, timeouts/review_environment wiring, .github/workflows/review-lab.yml and failure artifact paths so Review Lab installs and runs Cypress only; (3) port the two renderer-sync telemetry ordering assertions from Playwright smoke to Cypress; (4) add Cypress axe WCAG A/AA capability and a deterministic screenshot/visual baseline capability; (5) add a protected Firefox Cypress run and explicitly retire WebKit rather than silently retaining Playwright for it; (6) remove runnable Playwright dependencies, lockfile entries, package scripts, all playwright config files, globalSetup, duplicated/ad-hoc Playwright product specs, dead dev.spec.ts/axe-playwright/Makefile references, bootstrap switches and ignore entries; update tests/test_e2e_harness_contract.py, tests/test_local_review_test_environment.py and related docs/contracts in lockstep; (7) preserve committed Stage 26 bake-off/GPU/visual/performance receipts and historical governance documents as static evidence, but archive/remove their runnable Playwright harnesses so no executable Playwright toolchain remains; (8) retain the existing branch-protection-required check context if renaming it in code alone would make the branch unmergeable, but make clear it is a compatibility label for a Cypress-only job; (9) use immutable action SHAs and Node 24-compatible action releases; do not add ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION; (10) run focused Python contracts, frontend lint/type/unit tests, Cypress Chrome and Firefox where available, Review Lab verification where feasible, and git diff --check. Do not deploy, access production, use secrets, mutate databases outside disposable Review Lab services, or create another PR. Commit and push all coherent changes to the existing target branch."}