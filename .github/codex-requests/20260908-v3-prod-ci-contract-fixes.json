{"task":"Update PR #628 branch codex/run-34202782475-1 only, from its current exact head badbcfbbc91c34c53797ab34ffb88b0e9ab78df4. Do not touch production, do not authorize promotion, and do not weaken acceptance/safety tests. Fix the three concrete CI failures from CI run 34214842960: (1) tests/test_ci_stall_detection.py::test_all_checked_workflow_files_exist — add .github/workflows/v3-production-application-deploy.yml to CHECKED_WORKFLOWS so every job in the new workflow remains subject to timeout-minutes validation; (2) tests/test_pull_request_acceptance_fail_closed.py::test_no_workflow_contains_raw_merge_or_gh_api_and_merge_authority_is_allowlisted — remove the raw `gh api` usage from .github/workflows/v3-production-application-deploy.yml and authenticate the canonical release run through existing repository-owned scripts/release/v3_release_run.py / bounded curl provenance tooling instead. Do not add an exception or allowlist for raw gh api. Preserve exact current-main, successful manual canonical release, repository, branch and head-SHA binding; (3) tests/test_v3_application_stack_decision.py::test_primary_authorities_do_not_restore_superseded_stage_or_checkpoint_claims — revise the README wording so the separate Contabo/live checkpoint remains clearly described without placing `apps/web` close enough to a `non-production` phrase to trigger the stale-foundation contract. Do not change the contract test. Re-run exactly those three failing tests, tests/test_v3_production_application_deployment.py, Ruff on touched Python, and YAML/shell syntax as relevant. Inspect the diff, commit and push only through the governed Codex lane. Do not merge or execute production operations.","mode":"implement","target_branch":"codex/run-34202782475-1"}