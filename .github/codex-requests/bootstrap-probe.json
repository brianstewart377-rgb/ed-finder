{"mode":"implement","task":"Fix the CI stall-detection contract exposed by draft PR #499. The backend unit suite fails tests/test_ci_stall_detection.py::test_all_checked_workflow_files_exist because the newly added .github/workflows/codex-laptop.yml is not explicitly covered by CHECKED_WORKFLOWS. Inspect codex-laptop.yml and the stall-detection contract, make the smallest correct change needed so the new workflow is covered without weakening the timeout contract, and run the focused test file (at minimum tests/test_ci_stall_detection.py) plus git diff --check. Do not modify workflow files, production code, or unrelated files. Do not access or deploy production. Spread independent verification across parallel agents only if useful for speed."}
