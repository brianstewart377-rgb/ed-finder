{
  "mode": "implement",
  "task": "Create a clean replacement branch for PR #530's canonical pull-request acceptance policy, including the real Codex P1 repair. The worker starts from origin/main. First fetch origin/codex/run-33424648916-1 and verify its exact current head is bc6c5d6ca2c7b603f5ba5e379cedff7caca0edb5. Reproduce that branch's full diff against origin/main into the working tree without making an intermediate commit (for example by applying a binary diff), then finish the coordinated repair described below. Stop if the named branch or SHA differs.\n\nThe existing branch already adds docs/development/pull-request-acceptance-policy.md, updates CLAUDE.md and .github/PULL_REQUEST_TEMPLATE.md, replaces the old Codex-only policy test, and deletes .github/workflows/dependabot-auto-merge.yml. Preserve those intended changes. Fix the remaining inconsistency completely and narrowly:\n\n1. In CLAUDE.md, remove the stale statement that .github/workflows/dependabot-auto-merge.yml still exists or can enable auto-merge. State instead that automatic Dependabot auto-merge is disabled fail-closed. It may only be reintroduced after a separate enforceable mechanism verifies the complete canonical latest-head acceptance evidence: all required checks, both Codex Review and Octopus Review on that exact SHA, paginated inspection of all review surfaces, and recorded finding dispositions. CI alone is never enough. Do not alter roadmap/current-lane text.\n2. Extend tests/test_pull_request_acceptance_policy.py with focused structural coverage proving the former workflow is absent, no tracked .github/workflows/*.yml or *.yaml invokes `gh pr merge --auto`, and CLAUDE.md retains the fail-closed reintroduction rule. Avoid brittle whole-paragraph matching.\n3. Update tests/test_ci_stall_detection.py only as needed to remove the deleted workflow from any exhaustive job-bearing workflow inventory. Do not weaken unrelated stall-detection coverage.\n4. Verify the canonical policy, PR template, and CLAUDE link still require both reviewers, latest-head SHA, all review surfaces, no unresolved substantive findings, and fail-closed reviewer errors.\n\nRun `python -m pytest -q tests/test_pull_request_acceptance_policy.py tests/test_ci_stall_detection.py`, relevant repository-hygiene tests, Ruff on changed Python tests, and `git diff --check`. End with changed files, exact test evidence, and limitations. Do not merge, deploy, touch product/runtime/data behavior, or perform unrelated cleanup."
}
