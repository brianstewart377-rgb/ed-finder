{
  "mode": "implement",
  "target_branch": "migration/svelte-go-live-candidate",
  "task": "Correct the Python 3.14 hardening on PR #601 so it follows the repository's locked V3 database-driver authority instead of preserving psycopg2. Technology authority is docs/development/v3-application-stack-decision.md: the V3 API PostgreSQL driver is asyncpg; importer/synchronous PostgreSQL code targets Psycopg 3; psycopg2-binary is to be retired as that synchronous code is ported. Current exact head aa48ff620152d7b85e5ab12ea9fb386509896367 introduced psycopg2-binary==2.9.12 into apps/api/pyproject.toml's test group/uv.lock solely because the broad backend suite collects legacy importer/tooling code, while scripts/checks/data_invariants.py still imports psycopg2. Exact-head CI then fails tests/test_deploy_main_invariants_gate.py::test_invariant_wrapper_still_accepts_the_bounded_waiver_flags because scripts/run_data_invariants_receipted.sh launches system python3, which cannot import psycopg2. Do NOT fix this by making psycopg2 visible to the wrapper, by adding another psycopg2 install, or by cementing psycopg2 into the V3 frozen runtime/test graph. Instead make the smallest architecture-correct correction: (1) remove psycopg2-binary from the V3 apps/api pyproject/uv.lock graph; (2) port release/checkpoint-relevant synchronous PostgreSQL tooling exercised by the Python 3.14 exact-head gates, beginning with scripts/checks/data_invariants.py and any directly required companion path, to Psycopg 3 with equivalent fail-closed/read-only semantics; (3) if the V3 API unit job only required psycopg2 because it was also collecting legacy importer tests, separate that legacy tooling validation from the V3 CPython 3.14 API runtime proof rather than contaminating the V3 dependency graph. Preserve meaningful legacy importer coverage in its appropriate non-runtime/tooling lane; do not silently drop tests. Do not attempt an unbounded repo-wide importer rewrite unless the evidence shows it is required to keep those existing tests meaningful. Record any remaining psycopg2 importer files explicitly as bounded migration debt rather than installing psycopg2 into the V3 graph. The V3 API itself remains asyncpg; do not rewrite it to psycopg. Preserve PostgreSQL 18, uv/frozen-lock/runtime identity work, release provenance checks, Product E2E and Review Lab semantics, and deployment topology. Validate the formerly failing invariant-wrapper test under CPython 3.14, the V3 backend unit/coverage gates, relevant synchronous-tooling tests, uv lock/frozen sync, and runtime identity. Expected target parent is exactly aa48ff620152d7b85e5ab12ea9fb386509896367; trusted push must fail closed if migration/svelte-go-live-candidate has drifted. Commit and trusted-push only the bounded correction. Report changed files, exact psycopg2 references removed from the V3 graph, Psycopg 3 paths ported, any legacy psycopg2 debt deliberately left outside the V3 graph, validations, and pushed SHA. Do not touch production. Contabo is not production. This request supersedes worker run 34029458957's narrower interpreter-plumbing diagnosis; do not reuse that workaround if its output appears."
}