{"mode":"implement","task":"Implement ONLY the bounded Contabo V3 live-checkpoint provisioning operator, not the release/deploy dispatcher and not target-authority authorization itself. Base on current main. The non-production target is exactly hostname vmi3542235 with three existing GitHub runner services that must remain active. Add a request-triggered/allowlisted GitHub Actions provisioning control plane and source-free target bootstrap that fails closed unless hostname/FQDN/architecture and the exact three runner services match the recorded target. Provision idempotently: Docker Engine + Compose, psql/PostgreSQL 18 as a separately owned persistent NON-PRODUCTION database source, external network edfinder-v3-checkpoint-app, durable deployment receipt directory, secure API env file, local Docker context/config directory, and a loopback app origin plus a bounded NON-PRODUCTION HTTP edge route for vmi3542235.contaboserver.net without touching production/DNS. Reuse the repository's canonical scripts/apply_migrations.sh --include-manual, sql/seed_preview.sql, and seed acceptance/invariants so Finder/search/detail/map have the existing preview dataset; never copy production data. Generate DB/application credentials on-target, never print/artifact values, strict owners/modes. Generate a sanitized fresh schema identity receipt that matches current sql/migration-manifest.txt and prove it read-only against the new DB. For GHCR, probe/record only a real authority: anonymous/public pull if proven; otherwise emit a sanitized blocker/candidate that requires explicit secret-backed authority—do not invent credentials. Output only a sanitized provisioning receipt and target-authority candidate artifact suitable for a reviewed follow-up commit; do NOT directly change deploy/v3-live-checkpoint/target-authority.json. No Redis/Valkey/NATS. No application deploy, no production mutation, no git pull/build on the target. Add focused fail-closed tests and concise ops docs. Push a new isolated branch and seal normally.","target_branch":""}