{
  "task": "Repair only the three diagnosed exact-head acceptance failures on PR #601 branch migration/svelte-go-live-candidate. This is acceptance/test/control-plane reconciliation only: DO NOT change apps/web product behaviour, Finder/Babylon/Inspect behaviour, architecture, release scope, NATS, Redis/Valkey, production infrastructure, schemas, or production data. Start from the current target-branch head and preserve the existing two-parent main reconciliation. Required fixes: (1) docs/development/chatgpt-ops-control-plane.md must restore the branch's fail-closed historical authority banner containing exactly 'DESIGN/HISTORICAL DOCUMENT — NOT AN OPERATOR RUNBOOK.' and restore the heading 'Proposed operation set (not an implementation claim)', while retaining the merged current-main statement that the dispatcher and worker select exactly CPython 3.14 and that the dispatcher selects it before using Python. Update tests/test_codex_control_plane_python314_merge.py only as needed so it locks both the historical/non-operator authority and exact CPython 3.14 merged guarantee. (2) tests/test_python314_execution_surfaces.py must stop recursively treating generated dependency trees such as .venv and node_modules as repository execution surfaces; make the exclusion narrowly path-part based and retain scanning of real source/operational surfaces. (3) tests/test_station_type_canonical_pilot_postgres.py must make disposable Psycopg 3 CREATE ROLE password DDL valid by composing the generated password as a psycopg.sql.Literal rather than a %s parameter placeholder in CREATE ROLE; do not weaken role isolation or permissions. Run the focused non-Postgres tests that failed, plus the merge-regression test and Python-surface test. If a confirmed disposable canonical Postgres DSN is available in the governed environment, run the focused Postgres rehearsal too; otherwise leave its CI execution to the existing isolated canonical job. Keep the diff minimal and report exact files/tests changed. Do not fix unrelated failures or refactor adjacent code.",
  "mode": "implement",
  "target_branch": "migration/svelte-go-live-candidate"
}
