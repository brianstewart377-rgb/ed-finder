{
  "mode": "implement",
  "target_branch": "codex/run-34097461617-1",
  "task": "Fix only the single current Semgrep blocker on PR #627 exact head d870e06d06e27eacd35740114c795e6857f25758. Semgrep rule python.lang.security.audit.dynamic-urllib-use-detected.dynamic-urllib-use-detected blocks scripts/operator/install_v3_checkpoint_host_interface.py at urllib.request.urlopen in resolve_trusted_main_sha(). Replace the dynamic urllib URL handling with a fail-closed standard-library HTTPS-only connection to the fixed host api.github.com and fixed repository path /repos/brianstewart377-rgb/ed-finder/commits/main (http.client.HTTPSConnection is suitable). Preserve the 20 second timeout, Accept/User-Agent headers, HTTP 200 requirement, 1 MiB response bound, JSON validation, exact SHA validation, and all existing privilege/install semantics. Do not add dependencies, suppress Semgrep, change deployment architecture, or touch production. Because installer bytes change, recompute and update only the installer entry in scripts/operator/v3_checkpoint_host_interface.sha256; launcher/bootstrap digests must remain unchanged. Run the focused checkpoint privilege/local-transport tests plus Semgrep if available, Ruff and py_compile. Push only this minimal correction to the existing PR branch via the trusted push path." 
}
