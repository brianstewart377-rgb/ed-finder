# Stage 26C Evidence

This directory retains evidence for the isolated region-first R3F foundation.

- `region-first-1440x900.png` is the 2026-07-22 desktop visual-review capture.
  It shows the development-only 500k fixture with 42 authoritative region
  labels loaded, runtime-derived boundaries, a 25k background draw cap, five
  guaranteed systems, and the cluster-context companion.

The capture derives region geometry from ED-Finder's existing
`apps/importer/src/data/region_map.json` at development-server startup. It does
not add a second pixel-geometry source or resolve the legal uncertainty carried
by Stage 26B.
